{"config":{"lang":["en"],"separator":"[\\s\\-,:!=\\[\\]()\"/]+|(?!\\b)(?=[A-Z][a-z])|^(?=[^a-z])","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"MetaTrader5 Wrapper","text":"<p>Typed, Pythonic access to the official <code>MetaTrader5</code> Python package.</p> <p></p> <p><code>metatrader5_wrapper</code> keeps the underlying MT5 behavior visible while making results safer and easier to use in application code. Raw terminal calls are wrapped in typed result models so you can handle success and failure without depending on mutable global error state, and the client adds a fail-fast path for the common happy flow.</p>"},{"location":"#why-use-it","title":"Why use it","text":"<ul> <li>typed public API for editors and static type checkers</li> <li>Pydantic models for validated inputs and structured outputs</li> <li>MT5 error details captured immediately after each operation</li> <li>both explicit result-style and fail-fast helper-style workflows</li> <li>both function-style and client-style interfaces</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install metatrader5-wrapper\n</code></pre>"},{"location":"#quick-example","title":"Quick example","text":"<pre><code>from pydantic import SecretStr\n\nfrom metatrader5_wrapper import LoginCredential, MetaTrader5Client\n\ncredentials = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"password\"),\n    server=\"Broker-Demo\",\n    timeout=60,\n)\n\nwith MetaTrader5Client(credentials) as mt5:\n    account = mt5.account()\n    positions = mt5.positions(symbol=\"EURUSD\")\n\n    print(account.balance)\n    print(len(positions))\n</code></pre> <p>If you want to keep branching explicit, the result models stay available:</p> <pre><code>from metatrader5_wrapper import initialize\n\nresult = initialize()\n\nif result.failed:\n    print(result.describe_error())\n</code></pre>"},{"location":"#documentation-map","title":"Documentation map","text":"<ul> <li>Start with Getting Started</li> <li>Read Connection for terminal lifecycle behavior</li> <li>Read Results Model for <code>ConnectionResult</code> and <code>OperationResult</code></li> <li>Read Client vs Functions to choose your style</li> <li>Use API Reference for generated signatures and docstrings</li> </ul>"},{"location":"#current-api-surface","title":"Current API surface","text":"<ul> <li>connection: <code>initialize</code>, <code>login</code>, <code>shutdown</code></li> <li>terminal: <code>terminal_info</code>, <code>version</code></li> <li>account: <code>account_info</code></li> <li>symbols: <code>symbols_total</code>, <code>symbols_get</code>, <code>symbol_info</code>, <code>symbol_info_tick</code>, <code>symbol_select</code></li> <li>positions: <code>positions_total</code>, <code>positions_get</code></li> <li>client facade: <code>MetaTrader5Client</code></li> </ul>"},{"location":"api_reference/","title":"API Reference","text":""},{"location":"api_reference/#next-steps","title":"Next Steps","text":"<p>Getting Started</p> <p><code>metatrader5_wrapper</code> is a typed facade over the official <code>MetaTrader5</code> Python package. Public functions call MetaTrader 5, immediately capture <code>MetaTrader5.last_error()</code>, and return Pydantic result models instead of relying on global MT5 error state.</p>"},{"location":"api_reference/#design","title":"Design","text":"<p>The package is split into three layers:</p> <ul> <li><code>metatrader5_wrapper._core</code>: thin raw MT5 calls that capture <code>last_error()</code></li> <li>domain services such as <code>connection.service</code> and <code>symbols.service</code>: convert   raw MT5 payloads into typed result objects</li> <li>public exports in <code>metatrader5_wrapper.__init__</code>: replacement-style functions,   models, constants, and <code>MetaTrader5Client</code></li> </ul> <p>The <code>_core</code> modules are internal. Prefer importing from <code>metatrader5_wrapper</code> or from the domain packages.</p>"},{"location":"api_reference/#logging","title":"Logging","text":"<p>The package uses Python's standard <code>logging</code> module with the logger name <code>metatrader5_wrapper</code>.</p> <p>The wrapper does not configure logging, add handlers, or change global logging settings. Applications can opt in with normal logging configuration:</p> <pre><code>import logging\n\nlogging.basicConfig(level=logging.INFO)\n</code></pre> <p>Service functions log:</p> <ul> <li><code>DEBUG</code> before calling MT5</li> <li><code>INFO</code> when an operation succeeds</li> <li><code>WARNING</code> when MT5 reports a failed operation</li> </ul> <p>Credentials are not logged. Account login numbers, symbol names, filters, MT5 error codes, and MT5 error messages may be logged.</p>"},{"location":"api_reference/#results","title":"Results","text":""},{"location":"api_reference/#operationresultt","title":"OperationResult[T]","text":"<p>Generic result returned by account, terminal, symbol, and position operations.</p> <p>Fields:</p> <ul> <li><code>success</code>: <code>True</code> when the operation succeeded</li> <li><code>data</code>: typed payload, or <code>None</code> on failure</li> <li><code>error_code</code>: raw MT5 error code captured after the call</li> <li><code>error_message</code>: raw MT5 error message captured after the call</li> <li><code>message</code>: wrapper-level human-readable message</li> <li><code>failed</code>: property returning <code>not success</code></li> </ul>"},{"location":"api_reference/#connectionresult","title":"ConnectionResult","text":"<p>Result returned by <code>initialize()</code> and <code>login()</code>.</p> <p>Fields:</p> <ul> <li><code>success</code></li> <li><code>error_code</code></li> <li><code>error_message</code></li> <li><code>stage</code>: <code>ConnectionStage.INITIALIZE</code> or <code>ConnectionStage.LOGIN</code></li> <li><code>message</code></li> <li><code>failed</code>: property returning <code>not success</code></li> </ul>"},{"location":"api_reference/#connection","title":"Connection","text":""},{"location":"api_reference/#logincredential","title":"LoginCredential","text":"<p>Typed connection input.</p> <p>Fields:</p> <ul> <li><code>terminal_path</code>: optional path to a terminal executable</li> <li><code>login</code>: MT5 account number</li> <li><code>password</code>: <code>pydantic.SecretStr</code></li> <li><code>server</code>: MT5 trade server name</li> <li><code>timeout</code>: connection timeout in seconds, default <code>60</code></li> <li><code>portable</code>: whether MT5 should run in portable mode</li> <li><code>timeout_ms</code>: property used internally by the MT5 API</li> </ul>"},{"location":"api_reference/#initializecredentialsnone","title":"initialize(credentials=None)","text":"<p>Initializes the MT5 terminal. When credentials are provided, it initializes the terminal first and then logs in to the account.</p> <p>Returns <code>ConnectionResult</code>.</p>"},{"location":"api_reference/#logincredentials","title":"login(credentials)","text":"<p>Logs in to an account after the terminal has already been initialized.</p> <p>Returns <code>ConnectionResult</code>.</p>"},{"location":"api_reference/#shutdown","title":"shutdown()","text":"<p>Disconnects from the MT5 terminal.</p> <p>Returns <code>OperationResult[None]</code>.</p>"},{"location":"api_reference/#terminal","title":"Terminal","text":""},{"location":"api_reference/#terminal_info","title":"terminal_info()","text":"<p>Returns terminal details as <code>OperationResult[TerminalInfo]</code>.</p> <p><code>TerminalInfo</code> contains fields such as connection status, trading permissions, build number, path values, company, language, and terminal name.</p>"},{"location":"api_reference/#version","title":"version()","text":"<p>Returns terminal version details as <code>OperationResult[TerminalVersion]</code>.</p> <p><code>TerminalVersion</code> fields:</p> <ul> <li><code>version</code></li> <li><code>build</code></li> <li><code>release_date</code></li> </ul>"},{"location":"api_reference/#account","title":"Account","text":""},{"location":"api_reference/#account_info","title":"account_info()","text":"<p>Returns active account details as <code>OperationResult[AccountInfo]</code>.</p> <p><code>AccountInfo</code> contains account identity, leverage, trade permissions, margin mode, balances, margin values, currency, server, and broker company.</p>"},{"location":"api_reference/#symbols","title":"Symbols","text":""},{"location":"api_reference/#symbols_total","title":"symbols_total()","text":"<p>Returns the number of symbols available in the terminal as <code>OperationResult[int]</code>.</p>"},{"location":"api_reference/#symbols_getgroupnone","title":"symbols_get(group=None)","text":"<p>Returns available symbols as <code>OperationResult[list[SymbolInfo]]</code>.</p> <p><code>group</code> is passed through to the official MT5 <code>symbols_get(group=...)</code> filter.</p>"},{"location":"api_reference/#symbol_infosymbol","title":"symbol_info(symbol)","text":"<p>Returns one symbol as <code>OperationResult[SymbolInfo]</code>.</p> <p><code>SymbolInfo</code> includes common instrument metadata such as <code>name</code>, <code>description</code>, <code>path</code>, currencies, digits, spread, visibility, trading modes, volume limits, point size, and latest quoted prices when MT5 provides them.</p>"},{"location":"api_reference/#symbol_info_ticksymbol","title":"symbol_info_tick(symbol)","text":"<p>Returns the latest tick as <code>OperationResult[Tick]</code>.</p> <p><code>Tick</code> fields:</p> <ul> <li><code>time</code></li> <li><code>bid</code></li> <li><code>ask</code></li> <li><code>last</code></li> <li><code>volume</code></li> <li><code>time_msc</code></li> <li><code>flags</code></li> <li><code>volume_real</code></li> </ul>"},{"location":"api_reference/#symbol_selectsymbol-enabletrue","title":"symbol_select(symbol, enable=True)","text":"<p>Adds or removes a symbol from MarketWatch.</p> <p>Returns <code>OperationResult[None]</code>.</p>"},{"location":"api_reference/#positions","title":"Positions","text":""},{"location":"api_reference/#positions_total","title":"positions_total()","text":"<p>Returns the number of open positions as <code>OperationResult[int]</code>.</p>"},{"location":"api_reference/#positions_getsymbolnone-groupnone-ticketnone","title":"positions_get(symbol=None, group=None, ticket=None)","text":"<p>Returns open positions as <code>OperationResult[list[Position]]</code>.</p> <p>Only one MT5 filter is used at a time, matching the current implementation order:</p> <ul> <li><code>symbol</code></li> <li><code>group</code></li> <li><code>ticket</code></li> <li>no filter</li> </ul> <p><code>Position</code> includes ticket, open/update times, order type, magic number, identifier, reason, volume, prices, swap, profit, symbol, comment, and optional external ID.</p>"},{"location":"api_reference/#client-facade","title":"Client Facade","text":"<p><code>MetaTrader5Client</code> groups the same public operations behind an object-oriented interface.</p> <pre><code>from metatrader5_wrapper import MetaTrader5Client\n\nwith MetaTrader5Client(credentials) as mt5:\n    account = mt5.account_info()\n    symbols = mt5.symbols_get(group=\"*USD*\")\n</code></pre> <p>The context manager calls <code>initialize()</code> on entry and <code>shutdown()</code> on exit. If initialization fails, it raises <code>RuntimeError</code> with the connection result message and MT5 error details.</p>"},{"location":"api_reference/#mt5-constants","title":"MT5 Constants","text":"<p>Uppercase attributes not defined by the wrapper are forwarded to the official <code>MetaTrader5</code> module. This allows existing code to access constants such as order, position, and timeframe values while typed wrappers are added over time.</p> <pre><code>import metatrader5_wrapper as mt5\n\ntimeframe = mt5.TIMEFRAME_M1\n</code></pre>"},{"location":"api_reference/#development-commands","title":"Development Commands","text":"<pre><code>uv sync --all-groups\nuv run pytest\nuv run ruff check src tests\nuv run ruff format src tests\nuv run mypy src\n</code></pre>"},{"location":"connection_design/","title":"Connection Design Notes","text":"<p>MT5 Python API has untyped, unclear connection/login behavior. We provide a deterministic, typed connection interface.</p>"},{"location":"connection_design/#next-decisions","title":"Next Decisions","text":""},{"location":"connection_design/#1-error-structure","title":"1. Error Structure","text":"<p>We need to define what information is exposed when MT5 fails.</p> <p>Fields to decide: - error_code (from mt5.last_error) - error_message - stage (\"init\" | \"login\")</p>"},{"location":"connection_design/#2-initialize-contract","title":"2. initialize() Contract","text":"<p>Current ambiguity: - Accepts None \u2192 unclear meaning</p> <p>Options: - Require credentials (simpler) - Allow None \u2192 reuse existing session (needs clear logic)</p> <p>Decision pending.</p>"},{"location":"connection_design/#3-mt5-error-integration","title":"3. MT5 Error Integration","text":"<p>Currently: - Errors are generic strings</p> <p>Problem: - Losing mt5.last_error() info</p> <p>Need: - Capture and expose MT5 error code + message</p>"},{"location":"connection_design/#next-steps","title":"Next Steps","text":"<p>Getting Started</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<p>API Reference</p>"},{"location":"getting-started/#new-features","title":"New Features","text":"<p>The wrapper now includes support for historical orders and deals.</p>"},{"location":"getting-started/#install","title":"Install","text":"<pre><code>pip install metatrader5-wrapper\n</code></pre>"},{"location":"getting-started/#build-the-docs","title":"Build the docs","text":"<p>The documentation site currently targets the MkDocs 1.x ecosystem. Material for MkDocs and the plugins used by this project are not compatible with MkDocs 2.0, so keep the docs toolchain on <code>mkdocs&lt;2</code>.</p> <pre><code>uv sync --all-groups\nuv run mkdocs build\n</code></pre>"},{"location":"getting-started/#recommended-path-use-the-client","title":"Recommended path: use the client","text":"<p>For most scripts, services, and bots, start with <code>MetaTrader5Client</code>. It keeps connection lifecycle scoped and makes the happy path short.</p> <pre><code>from pydantic import SecretStr\n\nfrom metatrader5_wrapper import LoginCredential, MetaTrader5Client\n\ncredentials = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"password\"),\n    server=\"Broker-Demo\",\n    timeout=60,\n)\n\nwith MetaTrader5Client(credentials) as mt5:\n    account = mt5.account()\n    terminal = mt5.terminal()\n\n    print(account.login, account.balance)\n    print(terminal.name, terminal.connected)\n    history_orders = mt5.history_orders_get(datetime(2021, 1, 1), datetime(2021, 1, 10))\n    history_deals = mt5.history_deals_get(datetime(2021, 1, 1), datetime(2021, 1, 10))\n</code></pre>"},{"location":"getting-started/#explicit-result-flow","title":"Explicit result flow","text":"<p>If an existing MetaTrader 5 session is already available, you can initialize without explicit credentials.</p> <pre><code>from metatrader5_wrapper import initialize, shutdown\n\nresult = initialize()\n\nresult.expect(\"Could not initialize MetaTrader 5\")\n\nshutdown()\n</code></pre>"},{"location":"getting-started/#initialize-with-credentials","title":"Initialize with credentials","text":"<pre><code>from pydantic import SecretStr\n\nfrom metatrader5_wrapper import LoginCredential, initialize\n\ncredentials = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"password\"),\n    server=\"Broker-Demo\",\n    timeout=60,\n)\n\nresult = initialize(credentials)\n\nif result.failed:\n    print(result.describe_error())\n</code></pre>"},{"location":"getting-started/#fail-fast-convenience-helpers","title":"Fail-fast convenience helpers","text":"<pre><code>from metatrader5_wrapper import MetaTrader5Client\n\nwith MetaTrader5Client(credentials) as mt5:\n    account = mt5.account()\n    eurusd = mt5.symbol(\"EURUSD\")\n    tick = mt5.tick(\"EURUSD\")\n\n    print(account.balance)\n    print(eurusd.name, tick.bid, tick.ask)\n</code></pre>"},{"location":"getting-started/#what-to-read-next","title":"What to read next","text":"<ul> <li>Connection</li> <li>Client vs Functions</li> <li>API Reference</li> </ul>"},{"location":"api/","title":"API Reference","text":"<p>This section is generated from the package's public modules and docstrings. Use it for exact signatures, public models, and exported functions.</p>"},{"location":"api/#package-exports","title":"Package exports","text":""},{"location":"api/#metatrader5_wrapper","title":"metatrader5_wrapper","text":""},{"location":"api/#metatrader5_wrapper.AccountInfo","title":"AccountInfo","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed account details returned by <code>MetaTrader5.account_info()</code>.</p>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client","title":"MetaTrader5Client","text":"<pre><code>MetaTrader5Client(\n    credentials: LoginCredential | None = None,\n)\n</code></pre> <p>Pythonic client facade for typed MetaTrader 5 operations.</p> <p>Provides a clean, context-manager-friendly interface to MT5 connection, account, symbol, and position operations. All errors are captured as typed <code>ConnectionResult</code> or <code>OperationResult</code> objects\u2014no exceptions are raised for normal operation failures.</p> <p>Typical usage:</p> <pre><code>creds = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"password\"),\n    server=\"Broker-Demo\"\n)\nwith MetaTrader5Client(creds) as mt5:\n    account = mt5.account_info()\n    if account.success and account.data:\n        print(account.data.balance)\n</code></pre> <p>Initialize a MetaTrader5 client with optional credentials.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional LoginCredential for automatic connection on context manager entry. If <code>None</code>, credentials must be provided to <code>initialize()</code> or <code>login()</code> later.</p> <code>None</code> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def __init__(self, credentials: LoginCredential | None = None) -&gt; None:\n    \"\"\"Initialize a MetaTrader5 client with optional credentials.\n\n    Args:\n        credentials: Optional LoginCredential for automatic connection on\n            context manager entry. If ``None``, credentials must be provided\n            to ``initialize()`` or ``login()`` later.\n    \"\"\"\n    self.credentials = credentials\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.initialize","title":"initialize","text":"<pre><code>initialize(\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult\n</code></pre> <p>Initialize the terminal and optionally log in.</p> <p>If new credentials are provided, they replace any previously configured credentials for this client instance.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional LoginCredential. If provided, replaces the credentials stored on this client and is used for login.</p> <code>None</code> <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, connection stage, and error details.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def initialize(\n    self,\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult:\n    \"\"\"Initialize the terminal and optionally log in.\n\n    If new credentials are provided, they replace any previously configured\n    credentials for this client instance.\n\n    Args:\n        credentials: Optional LoginCredential. If provided, replaces the\n            credentials stored on this client and is used for login.\n\n    Returns:\n        ConnectionResult with success status, connection stage, and error details.\n    \"\"\"\n    if credentials is not None:\n        self.credentials = credentials\n\n    return initialize_connection(self.credentials)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.connect","title":"connect","text":"<pre><code>connect(\n    credentials: LoginCredential | None = None,\n) -&gt; MetaTrader5Client\n</code></pre> <p>Initialize the terminal and return the client on success.</p> <p>This is a convenience alias for application code that prefers a fluent, fail-fast startup path.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def connect(\n    self,\n    credentials: LoginCredential | None = None,\n) -&gt; \"MetaTrader5Client\":\n    \"\"\"Initialize the terminal and return the client on success.\n\n    This is a convenience alias for application code that prefers a fluent,\n    fail-fast startup path.\n    \"\"\"\n    result = self.initialize(credentials)\n    result.expect(\"Unable to connect to MetaTrader 5\")\n    return self\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.account_info","title":"account_info","text":"<pre><code>account_info() -&gt; OperationResult[AccountInfo]\n</code></pre> <p>Fetch current account details as a typed model.</p> <p>Requires an active, logged-in MT5 session.</p> <p>Returns:</p> Type Description <code>OperationResult[AccountInfo]</code> <p>OperationResult[AccountInfo] with typed account data on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def account_info(self) -&gt; OperationResult[AccountInfo]:\n    \"\"\"Fetch current account details as a typed model.\n\n    Requires an active, logged-in MT5 session.\n\n    Returns:\n        OperationResult[AccountInfo] with typed account data on success.\n    \"\"\"\n    return get_account_info()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.account","title":"account","text":"<pre><code>account() -&gt; AccountInfo\n</code></pre> <p>Return current account details or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def account(self) -&gt; AccountInfo:\n    \"\"\"Return current account details or raise on failure.\"\"\"\n    return self.account_info().expect(\"Unable to fetch account info\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.login","title":"login","text":"<pre><code>login(\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult\n</code></pre> <p>Log in to an MT5 account.</p> <p>Use this to defer login until after the terminal is initialized, or to switch between accounts.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional LoginCredential. If provided, replaces the credentials stored on this client.</p> <code>None</code> <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, stage, and error details.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no credentials have been configured on this client and none are provided here.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def login(self, credentials: LoginCredential | None = None) -&gt; ConnectionResult:\n    \"\"\"Log in to an MT5 account.\n\n    Use this to defer login until after the terminal is initialized, or to\n    switch between accounts.\n\n    Args:\n        credentials: Optional LoginCredential. If provided, replaces the\n            credentials stored on this client.\n\n    Returns:\n        ConnectionResult with success status, stage, and error details.\n\n    Raises:\n        ValueError: If no credentials have been configured on this client\n            and none are provided here.\n    \"\"\"\n    if credentials is not None:\n        self.credentials = credentials\n    if self.credentials is None:\n        raise ValueError(\"credentials are required to log in\")\n\n    return login_account(self.credentials)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.terminal_info","title":"terminal_info","text":"<pre><code>terminal_info() -&gt; OperationResult[TerminalInfo]\n</code></pre> <p>Fetch terminal information and status.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalInfo]</code> <p>OperationResult[TerminalInfo] with terminal metadata on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def terminal_info(self) -&gt; OperationResult[TerminalInfo]:\n    \"\"\"Fetch terminal information and status.\n\n    Returns:\n        OperationResult[TerminalInfo] with terminal metadata on success.\n    \"\"\"\n    return get_terminal_info()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.terminal","title":"terminal","text":"<pre><code>terminal() -&gt; TerminalInfo\n</code></pre> <p>Return terminal details or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def terminal(self) -&gt; TerminalInfo:\n    \"\"\"Return terminal details or raise on failure.\"\"\"\n    return self.terminal_info().expect(\"Unable to fetch terminal info\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.symbols_total","title":"symbols_total","text":"<pre><code>symbols_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the total number of tradeable symbols in the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the symbol count on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbols_total(self) -&gt; OperationResult[int]:\n    \"\"\"Count the total number of tradeable symbols in the terminal.\n\n    Returns:\n        OperationResult[int] with the symbol count on success.\n    \"\"\"\n    return get_symbols_total()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.symbols_get","title":"symbols_get","text":"<pre><code>symbols_get(\n    group: str | None = None,\n) -&gt; OperationResult[list[SymbolInfo]]\n</code></pre> <p>Fetch available trading symbols, optionally filtered by group.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str | None</code> <p>Optional MT5 group filter (e.g., \"USD\", \"CRYPTO*\").</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[SymbolInfo]]</code> <p>OperationResult[list[SymbolInfo]] with symbol list on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbols_get(\n    self, group: str | None = None\n) -&gt; OperationResult[list[SymbolInfo]]:\n    \"\"\"Fetch available trading symbols, optionally filtered by group.\n\n    Args:\n        group: Optional MT5 group filter (e.g., \"*USD*\", \"CRYPTO*\").\n\n    Returns:\n        OperationResult[list[SymbolInfo]] with symbol list on success.\n    \"\"\"\n    return get_symbols(group=group)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.symbols","title":"symbols","text":"<pre><code>symbols(group: str | None = None) -&gt; list[SymbolInfo]\n</code></pre> <p>Return trading symbols or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbols(self, group: str | None = None) -&gt; list[SymbolInfo]:\n    \"\"\"Return trading symbols or raise on failure.\"\"\"\n    return self.symbols_get(group=group).expect(\"Unable to fetch symbols\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.symbol_info","title":"symbol_info","text":"<pre><code>symbol_info(symbol: str) -&gt; OperationResult[SymbolInfo]\n</code></pre> <p>Fetch detailed information for a single trading symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\").</p> required <p>Returns:</p> Type Description <code>OperationResult[SymbolInfo]</code> <p>OperationResult[SymbolInfo] with typed symbol metadata on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol_info(self, symbol: str) -&gt; OperationResult[SymbolInfo]:\n    \"\"\"Fetch detailed information for a single trading symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\").\n\n    Returns:\n        OperationResult[SymbolInfo] with typed symbol metadata on success.\n    \"\"\"\n    return get_symbol_info(symbol)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.symbol","title":"symbol","text":"<pre><code>symbol(symbol: str) -&gt; SymbolInfo\n</code></pre> <p>Return one trading symbol or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol(self, symbol: str) -&gt; SymbolInfo:\n    \"\"\"Return one trading symbol or raise on failure.\"\"\"\n    return self.symbol_info(symbol).expect(\n        f\"Unable to fetch symbol info for {symbol!r}\"\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.symbol_info_tick","title":"symbol_info_tick","text":"<pre><code>symbol_info_tick(symbol: str) -&gt; OperationResult[Tick]\n</code></pre> <p>Fetch the latest tick data for a symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\").</p> required <p>Returns:</p> Type Description <code>OperationResult[Tick]</code> <p>OperationResult[Tick] with bid, ask, time, and volume on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol_info_tick(self, symbol: str) -&gt; OperationResult[Tick]:\n    \"\"\"Fetch the latest tick data for a symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\").\n\n    Returns:\n        OperationResult[Tick] with bid, ask, time, and volume on success.\n    \"\"\"\n    return get_symbol_info_tick(symbol)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.tick","title":"tick","text":"<pre><code>tick(symbol: str) -&gt; Tick\n</code></pre> <p>Return the latest tick for <code>symbol</code> or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def tick(self, symbol: str) -&gt; Tick:\n    \"\"\"Return the latest tick for ``symbol`` or raise on failure.\"\"\"\n    return self.symbol_info_tick(symbol).expect(\n        f\"Unable to fetch latest tick for {symbol!r}\"\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.symbol_select","title":"symbol_select","text":"<pre><code>symbol_select(\n    symbol: str, enable: bool = True\n) -&gt; OperationResult[None]\n</code></pre> <p>Add or remove a symbol from the terminal's MarketWatch.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name.</p> required <code>enable</code> <code>bool</code> <p>If <code>True</code> (default), adds to MarketWatch. If <code>False</code>, removes.</p> <code>True</code> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating success or failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol_select(self, symbol: str, enable: bool = True) -&gt; OperationResult[None]:\n    \"\"\"Add or remove a symbol from the terminal's MarketWatch.\n\n    Args:\n        symbol: Symbol name.\n        enable: If ``True`` (default), adds to MarketWatch. If ``False``, removes.\n\n    Returns:\n        OperationResult[None] indicating success or failure.\n    \"\"\"\n    return select_symbol(symbol, enable=enable)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.positions_total","title":"positions_total","text":"<pre><code>positions_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the number of currently open positions.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the position count on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def positions_total(self) -&gt; OperationResult[int]:\n    \"\"\"Count the number of currently open positions.\n\n    Returns:\n        OperationResult[int] with the position count on success.\n    \"\"\"\n    return get_positions_total()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.positions_get","title":"positions_get","text":"<pre><code>positions_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]\n</code></pre> <p>Fetch open positions, optionally filtered by symbol, group, or ticket.</p> <p>The filter is applied in priority order: symbol, then group, then ticket.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str | None</code> <p>Optional symbol name to filter positions.</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Optional group filter using MT5 wildcards.</p> <code>None</code> <code>ticket</code> <code>int | None</code> <p>Optional position ticket number for exact lookup.</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[Position]]</code> <p>OperationResult[list[Position]] with typed positions on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def positions_get(\n    self,\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]:\n    \"\"\"Fetch open positions, optionally filtered by symbol, group, or ticket.\n\n    The filter is applied in priority order: symbol, then group, then ticket.\n\n    Args:\n        symbol: Optional symbol name to filter positions.\n        group: Optional group filter using MT5 wildcards.\n        ticket: Optional position ticket number for exact lookup.\n\n    Returns:\n        OperationResult[list[Position]] with typed positions on success.\n    \"\"\"\n    return get_positions(symbol=symbol, group=group, ticket=ticket)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.positions","title":"positions","text":"<pre><code>positions(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; list[Position]\n</code></pre> <p>Return positions or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def positions(\n    self,\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; list[Position]:\n    \"\"\"Return positions or raise on failure.\"\"\"\n    return self.positions_get(\n        symbol=symbol,\n        group=group,\n        ticket=ticket,\n    ).expect(\"Unable to fetch positions\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.version","title":"version","text":"<pre><code>version() -&gt; OperationResult[TerminalVersion]\n</code></pre> <p>Fetch terminal version and build information.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalVersion]</code> <p>OperationResult[TerminalVersion] with version details on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def version(self) -&gt; OperationResult[TerminalVersion]:\n    \"\"\"Fetch terminal version and build information.\n\n    Returns:\n        OperationResult[TerminalVersion] with version details on success.\n    \"\"\"\n    return get_version()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.terminal_version","title":"terminal_version","text":"<pre><code>terminal_version() -&gt; TerminalVersion\n</code></pre> <p>Return terminal version details or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def terminal_version(self) -&gt; TerminalVersion:\n    \"\"\"Return terminal version details or raise on failure.\"\"\"\n    return self.version().expect(\"Unable to fetch terminal version\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.MetaTrader5Client.shutdown","title":"shutdown","text":"<pre><code>shutdown() -&gt; OperationResult[None]\n</code></pre> <p>Cleanly disconnect from the MetaTrader 5 terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating shutdown completed.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def shutdown(self) -&gt; OperationResult[None]:\n    \"\"\"Cleanly disconnect from the MetaTrader 5 terminal.\n\n    Returns:\n        OperationResult[None] indicating shutdown completed.\n    \"\"\"\n    return shutdown_terminal()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.ConnectionResult","title":"ConnectionResult","text":"<p>               Bases: <code>BaseModel</code></p> <p>Structured result for an MT5 connection attempt.</p>"},{"location":"api/#metatrader5_wrapper.ConnectionResult.failed","title":"failed  <code>property</code>","text":"<pre><code>failed: bool\n</code></pre> <p>Return <code>True</code> when the connection operation did not succeed.</p>"},{"location":"api/#metatrader5_wrapper.ConnectionResult.unwrap","title":"unwrap","text":"<pre><code>unwrap() -&gt; None\n</code></pre> <p>Return <code>None</code> on success or raise <code>RuntimeError</code> on failure.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\models.py</code> <pre><code>def unwrap(self) -&gt; None:\n    \"\"\"Return ``None`` on success or raise ``RuntimeError`` on failure.\"\"\"\n    if self.success:\n        return\n    raise RuntimeError(self.describe_error())\n</code></pre>"},{"location":"api/#metatrader5_wrapper.ConnectionResult.expect","title":"expect","text":"<pre><code>expect(message: str) -&gt; None\n</code></pre> <p>Return <code>None</code> on success or raise <code>RuntimeError</code> with context.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\models.py</code> <pre><code>def expect(self, message: str) -&gt; None:\n    \"\"\"Return ``None`` on success or raise ``RuntimeError`` with context.\"\"\"\n    if self.success:\n        return\n    raise RuntimeError(f\"{message}: {self.describe_error()}\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.ConnectionResult.describe_error","title":"describe_error","text":"<pre><code>describe_error() -&gt; str\n</code></pre> <p>Return a human-readable summary of the connection failure.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\models.py</code> <pre><code>def describe_error(self) -&gt; str:\n    \"\"\"Return a human-readable summary of the connection failure.\"\"\"\n    base_message = self.message or \"MetaTrader 5 connection failed\"\n    return (\n        f\"{base_message} during {self.stage} [{self.error_code}]\"\n        f\" {self.error_message}\"\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.ConnectionStage","title":"ConnectionStage","text":"<p>               Bases: <code>StrEnum</code></p> <p>Connection operation stage associated with the result.</p>"},{"location":"api/#metatrader5_wrapper.LoginCredential","title":"LoginCredential","text":"<p>               Bases: <code>BaseModel</code></p> <p>Credentials and terminal options used to connect to an MT5 account.</p>"},{"location":"api/#metatrader5_wrapper.LoginCredential.timeout_ms","title":"timeout_ms  <code>property</code>","text":"<pre><code>timeout_ms: int\n</code></pre> <p>Return the configured timeout in milliseconds for the MT5 API.</p>"},{"location":"api/#metatrader5_wrapper.OperationResult","title":"OperationResult","text":"<p>               Bases: <code>BaseModel</code>, <code>Generic[T]</code></p> <p>Generic structured result for MT5 operations.</p> <p>The model keeps MT5's explicit success and error semantics, while adding a few convenience helpers for application code.</p>"},{"location":"api/#metatrader5_wrapper.OperationResult.failed","title":"failed  <code>property</code>","text":"<pre><code>failed: bool\n</code></pre> <p>Return <code>True</code> when the operation did not succeed.</p>"},{"location":"api/#metatrader5_wrapper.OperationResult.unwrap","title":"unwrap","text":"<pre><code>unwrap() -&gt; T\n</code></pre> <p>Return data or raise <code>RuntimeError</code> with MT5 error details.</p> Source code in <code>src\\metatrader5_wrapper\\models.py</code> <pre><code>def unwrap(self) -&gt; T:\n    \"\"\"Return data or raise ``RuntimeError`` with MT5 error details.\"\"\"\n    if self.success and self.data is not None:\n        return self.data\n    raise RuntimeError(self.describe_error())\n</code></pre>"},{"location":"api/#metatrader5_wrapper.OperationResult.expect","title":"expect","text":"<pre><code>expect(message: str) -&gt; T\n</code></pre> <p>Return data or raise <code>RuntimeError</code> prefixed with <code>message</code>.</p> Source code in <code>src\\metatrader5_wrapper\\models.py</code> <pre><code>def expect(self, message: str) -&gt; T:\n    \"\"\"Return data or raise ``RuntimeError`` prefixed with ``message``.\"\"\"\n    if self.success and self.data is not None:\n        return self.data\n    raise RuntimeError(f\"{message}: {self.describe_error()}\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.OperationResult.describe_error","title":"describe_error","text":"<pre><code>describe_error() -&gt; str\n</code></pre> <p>Return a human-readable error summary for logs and exceptions.</p> Source code in <code>src\\metatrader5_wrapper\\models.py</code> <pre><code>def describe_error(self) -&gt; str:\n    \"\"\"Return a human-readable error summary for logs and exceptions.\"\"\"\n    base_message = self.message or \"MetaTrader 5 operation failed\"\n    if self.error_code is None and self.error_message is None:\n        return base_message\n    return f\"{base_message} [{self.error_code}] {self.error_message}\"\n</code></pre>"},{"location":"api/#metatrader5_wrapper.TradeCheckResult","title":"TradeCheckResult","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed result returned by <code>MetaTrader5.order_check()</code>.</p>"},{"location":"api/#metatrader5_wrapper.TradeOrder","title":"TradeOrder","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed active order payload returned by <code>MetaTrader5.orders_get()</code>.</p>"},{"location":"api/#metatrader5_wrapper.TradeSendResult","title":"TradeSendResult","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed result returned by <code>MetaTrader5.order_send()</code>.</p>"},{"location":"api/#metatrader5_wrapper.Position","title":"Position","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed position payload returned by <code>MetaTrader5.positions_get()</code>.</p>"},{"location":"api/#metatrader5_wrapper.SymbolInfo","title":"SymbolInfo","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed symbol details returned by <code>MetaTrader5.symbol_info()</code>.</p>"},{"location":"api/#metatrader5_wrapper.Tick","title":"Tick","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed latest-tick payload returned by <code>MetaTrader5.symbol_info_tick()</code>.</p>"},{"location":"api/#metatrader5_wrapper.TerminalInfo","title":"TerminalInfo","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed terminal details returned by <code>MetaTrader5.terminal_info()</code>.</p>"},{"location":"api/#metatrader5_wrapper.TerminalVersion","title":"TerminalVersion","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed terminal version payload returned by <code>MetaTrader5.version()</code>.</p>"},{"location":"api/#metatrader5_wrapper.account_info","title":"account_info","text":"<pre><code>account_info() -&gt; OperationResult[AccountInfo]\n</code></pre> <p>Fetch current account details as a typed model.</p> <p>Requires an active, logged-in MT5 session. Reads account balance, equity, margin, leverage, trading permissions, and account metadata from the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[AccountInfo]</code> <p>OperationResult[AccountInfo] with success status, typed AccountInfo data</p> <code>OperationResult[AccountInfo]</code> <p>on success, raw MT5 error code and message on failure, and a status message.</p> <p>Example::</p> <pre><code>result = account_info()\nif result.success and result.data:\n    print(f\"Balance: {result.data.balance}\")\n    print(f\"Equity: {result.data.equity}\")\n    print(f\"Margin: {result.data.margin}\")\nelse:\n    print(result.error_code, result.error_message)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\account\\service.py</code> <pre><code>def account_info() -&gt; OperationResult[AccountInfo]:\n    \"\"\"Fetch current account details as a typed model.\n\n    Requires an active, logged-in MT5 session. Reads account balance, equity,\n    margin, leverage, trading permissions, and account metadata from the\n    terminal.\n\n    Returns:\n        OperationResult[AccountInfo] with success status, typed AccountInfo data\n        on success, raw MT5 error code and message on failure, and a status message.\n\n    Example::\n\n        result = account_info()\n        if result.success and result.data:\n            print(f\"Balance: {result.data.balance}\")\n            print(f\"Equity: {result.data.equity}\")\n            print(f\"Margin: {result.data.margin}\")\n        else:\n            print(result.error_code, result.error_message)\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 account info.\")\n    result = mt5_account.account_info()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 account info: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[AccountInfo](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 account info.\",\n        )\n\n    account = model_from_namedtuple(AccountInfo, result.data)\n    logger.info(\"MetaTrader 5 account info fetched successfully.\")\n    return OperationResult[AccountInfo](\n        success=True,\n        data=account,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 account info fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.initialize","title":"initialize","text":"<pre><code>initialize(\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult\n</code></pre> <p>Initialize the MetaTrader 5 terminal and optionally log in.</p> <p>When called without credentials, initializes the terminal for use with an existing MT5 session or manual login later. When called with credentials, performs both initialization and account login as a single operation.</p> <p>The official MT5 module stores errors globally in <code>last_error()</code>. This wrapper captures that error state immediately after each call and returns it as part of the typed <code>ConnectionResult</code>, eliminating the need to check mutable global state.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional <code>LoginCredential</code> containing account number, password, server name, and connection timeout. If <code>None</code>, the terminal initializes without automatic login.</p> <code>None</code> <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, stage (initialize or login), raw MT5</p> <code>ConnectionResult</code> <p>error code and message, and a human-readable status message.</p> Example <p>Initialize without credentials::</p> <pre><code>result = initialize()\nif result.success:\n    print(\"Terminal ready\")\n</code></pre> <p>Initialize with credentials::</p> <pre><code>creds = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"mypassword\"),\n    server=\"Broker-Demo\"\n)\nresult = initialize(creds)\nif result.failed:\n    print(result.stage, result.error_code)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\connection\\service.py</code> <pre><code>def initialize(credentials: LoginCredential | None = None) -&gt; ConnectionResult:\n    \"\"\"Initialize the MetaTrader 5 terminal and optionally log in.\n\n    When called without credentials, initializes the terminal for use with an\n    existing MT5 session or manual login later. When called with credentials,\n    performs both initialization and account login as a single operation.\n\n    The official MT5 module stores errors globally in ``last_error()``. This\n    wrapper captures that error state immediately after each call and returns it\n    as part of the typed ``ConnectionResult``, eliminating the need to check\n    mutable global state.\n\n    Args:\n        credentials: Optional ``LoginCredential`` containing account number, password,\n            server name, and connection timeout. If ``None``, the terminal initializes\n            without automatic login.\n\n    Returns:\n        ConnectionResult with success status, stage (initialize or login), raw MT5\n        error code and message, and a human-readable status message.\n\n    Example:\n        Initialize without credentials::\n\n            result = initialize()\n            if result.success:\n                print(\"Terminal ready\")\n\n        Initialize with credentials::\n\n            creds = LoginCredential(\n                login=12345678,\n                password=SecretStr(\"mypassword\"),\n                server=\"Broker-Demo\"\n            )\n            result = initialize(creds)\n            if result.failed:\n                print(result.stage, result.error_code)\n    \"\"\"\n    logger.debug(\"Initializing MetaTrader 5 terminal.\")\n    init_result = mt5_connection.initialize_terminal(\n        terminal_path=credentials.terminal_path if credentials else None,\n        timeout_ms=credentials.timeout_ms if credentials else 60_000,\n        portable=credentials.portable if credentials else False,\n    )\n    if not init_result.success:\n        logger.warning(\n            \"MetaTrader 5 terminal initialization failed: [%s] %s\",\n            init_result.error.code,\n            init_result.error.message,\n        )\n        return _to_connection_result(\n            init_result,\n            stage=ConnectionStage.INITIALIZE,\n            message=\"Unable to initialize the MetaTrader 5 terminal.\",\n        )\n\n    if credentials is None:\n        logger.info(\"MetaTrader 5 terminal initialized successfully.\")\n        return _to_connection_result(\n            init_result,\n            stage=ConnectionStage.INITIALIZE,\n            message=\"MetaTrader 5 terminal initialized successfully.\",\n        )\n\n    logger.debug(\"Logging in to MetaTrader 5 account %s.\", credentials.login)\n    login_result = mt5_connection.login_account(\n        login=credentials.login,\n        password=credentials.password.get_secret_value(),\n        server=credentials.server,\n        timeout_ms=credentials.timeout_ms,\n    )\n    if not login_result.success:\n        logger.warning(\n            \"MetaTrader 5 account login failed for %s: [%s] %s\",\n            credentials.login,\n            login_result.error.code,\n            login_result.error.message,\n        )\n        return _to_connection_result(\n            login_result,\n            stage=ConnectionStage.LOGIN,\n            message=\"Unable to log in to the MetaTrader 5 account.\",\n        )\n\n    logger.info(\"MetaTrader 5 account %s connected successfully.\", credentials.login)\n    return _to_connection_result(\n        login_result,\n        stage=ConnectionStage.LOGIN,\n        message=\"MetaTrader 5 account connected successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.login","title":"login","text":"<pre><code>login(credentials: LoginCredential) -&gt; ConnectionResult\n</code></pre> <p>Log in to an MT5 account after the terminal has been initialized.</p> <p>Use this function when you want to defer login until after the terminal is already running, or when you need to switch between accounts in the same session.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential</code> <p>LoginCredential containing account number, password, server, and connection timeout.</p> required <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, stage set to LOGIN, raw MT5 error</p> <code>ConnectionResult</code> <p>code and message, and a descriptive status message.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\service.py</code> <pre><code>def login(credentials: LoginCredential) -&gt; ConnectionResult:\n    \"\"\"Log in to an MT5 account after the terminal has been initialized.\n\n    Use this function when you want to defer login until after the terminal\n    is already running, or when you need to switch between accounts in the\n    same session.\n\n    Args:\n        credentials: LoginCredential containing account number, password, server,\n            and connection timeout.\n\n    Returns:\n        ConnectionResult with success status, stage set to LOGIN, raw MT5 error\n        code and message, and a descriptive status message.\n    \"\"\"\n    logger.debug(\"Logging in to MetaTrader 5 account %s.\", credentials.login)\n    login_result = mt5_connection.login_account(\n        login=credentials.login,\n        password=credentials.password.get_secret_value(),\n        server=credentials.server,\n        timeout_ms=credentials.timeout_ms,\n    )\n    if not login_result.success:\n        logger.warning(\n            \"MetaTrader 5 account login failed for %s: [%s] %s\",\n            credentials.login,\n            login_result.error.code,\n            login_result.error.message,\n        )\n        return _to_connection_result(\n            login_result,\n            stage=ConnectionStage.LOGIN,\n            message=\"Unable to log in to the MetaTrader 5 account.\",\n        )\n\n    logger.info(\"MetaTrader 5 account %s connected successfully.\", credentials.login)\n    return _to_connection_result(\n        login_result,\n        stage=ConnectionStage.LOGIN,\n        message=\"MetaTrader 5 account connected successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.history_deals_get","title":"history_deals_get","text":"<pre><code>history_deals_get(\n    date_from: datetime,\n    date_to: datetime,\n    *,\n    group: str | None = None,\n    ticket: int | None = None,\n    position: int | None = None,\n) -&gt; OperationResult[list[HistoryDeal]]\n</code></pre> <p>Return historical deals in the given range or by ticket/position.</p> Source code in <code>src\\metatrader5_wrapper\\history\\service.py</code> <pre><code>def history_deals_get(\n    date_from: datetime,\n    date_to: datetime,\n    *,\n    group: str | None = None,\n    ticket: int | None = None,\n    position: int | None = None,\n) -&gt; OperationResult[list[HistoryDeal]]:\n    \"\"\"Return historical deals in the given range or by ticket/position.\"\"\"\n    result = mt5_history.history_deals_get(\n        date_from,\n        date_to,\n        group=group,\n        ticket=ticket,\n        position=position,\n    )\n    if not result.success:\n        return OperationResult[list[HistoryDeal]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 history deals.\",\n        )\n    return OperationResult[list[HistoryDeal]](\n        success=True,\n        data=models_from_namedtuples(HistoryDeal, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 history deals fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.history_deals_total","title":"history_deals_total","text":"<pre><code>history_deals_total(\n    date_from: datetime, date_to: datetime\n) -&gt; OperationResult[int]\n</code></pre> <p>Return the number of historical deals in a date range.</p> Source code in <code>src\\metatrader5_wrapper\\history\\service.py</code> <pre><code>def history_deals_total(\n    date_from: datetime,\n    date_to: datetime,\n) -&gt; OperationResult[int]:\n    \"\"\"Return the number of historical deals in a date range.\"\"\"\n    result = mt5_history.history_deals_total(date_from, date_to)\n    if not result.success:\n        return OperationResult[int](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 history deals total.\",\n        )\n    return OperationResult[int](\n        success=True,\n        data=int(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 history deals total fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.history_orders_get","title":"history_orders_get","text":"<pre><code>history_orders_get(\n    date_from: datetime,\n    date_to: datetime,\n    *,\n    group: str | None = None,\n    ticket: int | None = None,\n    position: int | None = None,\n) -&gt; OperationResult[list[HistoryOrder]]\n</code></pre> <p>Return historical orders in the given range or by ticket/position.</p> Source code in <code>src\\metatrader5_wrapper\\history\\service.py</code> <pre><code>def history_orders_get(\n    date_from: datetime,\n    date_to: datetime,\n    *,\n    group: str | None = None,\n    ticket: int | None = None,\n    position: int | None = None,\n) -&gt; OperationResult[list[HistoryOrder]]:\n    \"\"\"Return historical orders in the given range or by ticket/position.\"\"\"\n    result = mt5_history.history_orders_get(\n        date_from,\n        date_to,\n        group=group,\n        ticket=ticket,\n        position=position,\n    )\n    if not result.success:\n        return OperationResult[list[HistoryOrder]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 history orders.\",\n        )\n    return OperationResult[list[HistoryOrder]](\n        success=True,\n        data=models_from_namedtuples(HistoryOrder, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 history orders fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.history_orders_total","title":"history_orders_total","text":"<pre><code>history_orders_total(\n    date_from: datetime, date_to: datetime\n) -&gt; OperationResult[int]\n</code></pre> <p>Return the number of historical orders in a date range.</p> Source code in <code>src\\metatrader5_wrapper\\history\\service.py</code> <pre><code>def history_orders_total(\n    date_from: datetime,\n    date_to: datetime,\n) -&gt; OperationResult[int]:\n    \"\"\"Return the number of historical orders in a date range.\"\"\"\n    result = mt5_history.history_orders_total(date_from, date_to)\n    if not result.success:\n        return OperationResult[int](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 history orders total.\",\n        )\n    return OperationResult[int](\n        success=True,\n        data=int(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 history orders total fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.market_book_add","title":"market_book_add","text":"<pre><code>market_book_add(symbol: str) -&gt; OperationResult[None]\n</code></pre> <p>Subscribe to market depth change events for <code>symbol</code>.</p> Source code in <code>src\\metatrader5_wrapper\\market_book\\service.py</code> <pre><code>def market_book_add(symbol: str) -&gt; OperationResult[None]:\n    \"\"\"Subscribe to market depth change events for ``symbol``.\"\"\"\n    logger.debug(\"Subscribing to market depth for %r.\", symbol)\n    result = mt5_market_book.market_book_add(symbol)\n    if not result.success:\n        logger.warning(\n            \"Unable to subscribe to market depth for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[None](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to subscribe to market depth for {symbol!r}.\",\n        )\n\n    logger.info(\"Subscribed to market depth for %r.\", symbol)\n    return OperationResult[None](\n        success=True,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Subscribed to market depth for {symbol!r}.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.market_book_get","title":"market_book_get","text":"<pre><code>market_book_get(\n    symbol: str,\n) -&gt; OperationResult[list[object]]\n</code></pre> <p>Return market depth entries for <code>symbol</code>.</p> Source code in <code>src\\metatrader5_wrapper\\market_book\\service.py</code> <pre><code>def market_book_get(symbol: str) -&gt; OperationResult[list[object]]:\n    \"\"\"Return market depth entries for ``symbol``.\"\"\"\n    logger.debug(\"Fetching market depth for %r.\", symbol)\n    result = mt5_market_book.market_book_get(symbol)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch market depth for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[list[object]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to fetch market depth for {symbol!r}.\",\n        )\n\n    logger.info(\"Market depth for %r fetched successfully.\", symbol)\n    return OperationResult[list[object]](\n        success=True,\n        data=list(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Market depth for {symbol!r} fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.market_book_release","title":"market_book_release","text":"<pre><code>market_book_release(symbol: str) -&gt; OperationResult[None]\n</code></pre> <p>Cancel market depth subscription for <code>symbol</code>.</p> Source code in <code>src\\metatrader5_wrapper\\market_book\\service.py</code> <pre><code>def market_book_release(symbol: str) -&gt; OperationResult[None]:\n    \"\"\"Cancel market depth subscription for ``symbol``.\"\"\"\n    logger.debug(\"Releasing market depth subscription for %r.\", symbol)\n    result = mt5_market_book.market_book_release(symbol)\n    if not result.success:\n        logger.warning(\n            \"Unable to release market depth for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[None](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to release market depth for {symbol!r}.\",\n        )\n\n    logger.info(\"Released market depth subscription for %r.\", symbol)\n    return OperationResult[None](\n        success=True,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Released market depth subscription for {symbol!r}.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.order_calc_margin","title":"order_calc_margin","text":"<pre><code>order_calc_margin(\n    action: int, symbol: str, volume: float, price: float\n) -&gt; OperationResult[float]\n</code></pre> <p>Return margin required for a trading operation.</p> Source code in <code>src\\metatrader5_wrapper\\orders\\service.py</code> <pre><code>def order_calc_margin(\n    action: int,\n    symbol: str,\n    volume: float,\n    price: float,\n) -&gt; OperationResult[float]:\n    \"\"\"Return margin required for a trading operation.\"\"\"\n    result = mt5_orders.order_calc_margin(action, symbol, volume, price)\n    if not result.success:\n        return OperationResult[float](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to calculate MetaTrader 5 margin.\",\n        )\n    return OperationResult[float](\n        success=True,\n        data=float(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 margin calculated successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.order_calc_profit","title":"order_calc_profit","text":"<pre><code>order_calc_profit(\n    action: int,\n    symbol: str,\n    volume: float,\n    price_open: float,\n    price_close: float,\n) -&gt; OperationResult[float]\n</code></pre> <p>Return profit for a trading operation.</p> Source code in <code>src\\metatrader5_wrapper\\orders\\service.py</code> <pre><code>def order_calc_profit(\n    action: int,\n    symbol: str,\n    volume: float,\n    price_open: float,\n    price_close: float,\n) -&gt; OperationResult[float]:\n    \"\"\"Return profit for a trading operation.\"\"\"\n    result = mt5_orders.order_calc_profit(\n        action,\n        symbol,\n        volume,\n        price_open,\n        price_close,\n    )\n    if not result.success:\n        return OperationResult[float](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to calculate MetaTrader 5 profit.\",\n        )\n    return OperationResult[float](\n        success=True,\n        data=float(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 profit calculated successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.order_check","title":"order_check","text":"<pre><code>order_check(\n    request: dict[str, Any],\n) -&gt; OperationResult[TradeCheckResult]\n</code></pre> <p>Validate a trade request without sending it.</p> Source code in <code>src\\metatrader5_wrapper\\orders\\service.py</code> <pre><code>def order_check(request: dict[str, Any]) -&gt; OperationResult[TradeCheckResult]:\n    \"\"\"Validate a trade request without sending it.\"\"\"\n    result = mt5_orders.order_check(request)\n    if not result.success:\n        return OperationResult[TradeCheckResult](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to validate MetaTrader 5 order request.\",\n        )\n    return OperationResult[TradeCheckResult](\n        success=True,\n        data=TradeCheckResult.model_validate(result.data._asdict()),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 order request validated successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.order_send","title":"order_send","text":"<pre><code>order_send(\n    request: dict[str, Any],\n) -&gt; OperationResult[TradeSendResult]\n</code></pre> <p>Send a trade request.</p> Source code in <code>src\\metatrader5_wrapper\\orders\\service.py</code> <pre><code>def order_send(request: dict[str, Any]) -&gt; OperationResult[TradeSendResult]:\n    \"\"\"Send a trade request.\"\"\"\n    result = mt5_orders.order_send(request)\n    if not result.success:\n        return OperationResult[TradeSendResult](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to send MetaTrader 5 order request.\",\n        )\n    return OperationResult[TradeSendResult](\n        success=True,\n        data=TradeSendResult.model_validate(result.data._asdict()),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 order request sent successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.orders_get","title":"orders_get","text":"<pre><code>orders_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[TradeOrder]]\n</code></pre> <p>Return active orders using the supported MT5 filters.</p> Source code in <code>src\\metatrader5_wrapper\\orders\\service.py</code> <pre><code>def orders_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[TradeOrder]]:\n    \"\"\"Return active orders using the supported MT5 filters.\"\"\"\n    logger.debug(\n        \"Fetching MetaTrader 5 orders with symbol=%r group=%r ticket=%r.\",\n        symbol,\n        group,\n        ticket,\n    )\n    result = mt5_orders.orders_get(symbol=symbol, group=group, ticket=ticket)\n    if not result.success:\n        return OperationResult[list[TradeOrder]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 orders.\",\n        )\n    return OperationResult[list[TradeOrder]](\n        success=True,\n        data=models_from_namedtuples(TradeOrder, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 orders fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.orders_total","title":"orders_total","text":"<pre><code>orders_total() -&gt; OperationResult[int]\n</code></pre> <p>Return the number of active orders.</p> Source code in <code>src\\metatrader5_wrapper\\orders\\service.py</code> <pre><code>def orders_total() -&gt; OperationResult[int]:\n    \"\"\"Return the number of active orders.\"\"\"\n    result = mt5_orders.orders_total()\n    if not result.success:\n        return OperationResult[int](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 orders total.\",\n        )\n    return OperationResult[int](\n        success=True,\n        data=int(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 orders total fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.positions_get","title":"positions_get","text":"<pre><code>positions_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]\n</code></pre> <p>Fetch open positions, optionally filtered by symbol, group, or ticket.</p> <p>The filter is applied in priority order: symbol is checked first, then group, then ticket. Only one filter is used even if multiple are provided.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str | None</code> <p>Optional symbol name to filter positions (e.g., \"EURUSD\").</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Optional group filter using MT5 wildcards (e.g., \"USD\").</p> <code>None</code> <code>ticket</code> <code>int | None</code> <p>Optional position ticket number for exact lookup.</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[Position]]</code> <p>OperationResult[list[Position]] with typed position objects on success,</p> <code>OperationResult[list[Position]]</code> <p>or error details on failure.</p> <p>Example::</p> <pre><code># Get all open positions\nresult = positions_get()\n\n# Get positions for EURUSD only\nresult = positions_get(symbol=\"EURUSD\")\nif result.success and result.data:\n    for pos in result.data:\n        print(f\"Ticket: {pos.ticket}, Volume: {pos.volume}\")\n\n# Get a specific position by ticket\nresult = positions_get(ticket=12345)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\positions\\service.py</code> <pre><code>def positions_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]:\n    \"\"\"Fetch open positions, optionally filtered by symbol, group, or ticket.\n\n    The filter is applied in priority order: symbol is checked first, then group,\n    then ticket. Only one filter is used even if multiple are provided.\n\n    Args:\n        symbol: Optional symbol name to filter positions (e.g., \"EURUSD\").\n        group: Optional group filter using MT5 wildcards (e.g., \"*USD*\").\n        ticket: Optional position ticket number for exact lookup.\n\n    Returns:\n        OperationResult[list[Position]] with typed position objects on success,\n        or error details on failure.\n\n    Example::\n\n        # Get all open positions\n        result = positions_get()\n\n        # Get positions for EURUSD only\n        result = positions_get(symbol=\"EURUSD\")\n        if result.success and result.data:\n            for pos in result.data:\n                print(f\"Ticket: {pos.ticket}, Volume: {pos.volume}\")\n\n        # Get a specific position by ticket\n        result = positions_get(ticket=12345)\n    \"\"\"\n    logger.debug(\n        \"Fetching MetaTrader 5 positions with symbol=%r group=%r ticket=%r.\",\n        symbol,\n        group,\n        ticket,\n    )\n    result = mt5_positions.positions_get(symbol=symbol, group=group, ticket=ticket)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 positions: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[list[Position]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 positions.\",\n        )\n\n    logger.info(\"MetaTrader 5 positions fetched successfully.\")\n    return OperationResult[list[Position]](\n        success=True,\n        data=models_from_namedtuples(Position, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 positions fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.positions_total","title":"positions_total","text":"<pre><code>positions_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the number of currently open positions.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the position count on success.</p> <p>Example::</p> <pre><code>result = positions_total()\nif result.success:\n    print(f\"Open positions: {result.data}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\positions\\service.py</code> <pre><code>def positions_total() -&gt; OperationResult[int]:\n    \"\"\"Count the number of currently open positions.\n\n    Returns:\n        OperationResult[int] with the position count on success.\n\n    Example::\n\n        result = positions_total()\n        if result.success:\n            print(f\"Open positions: {result.data}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 positions total.\")\n    result = mt5_positions.positions_total()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 positions total: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[int](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 positions total.\",\n        )\n\n    logger.info(\"MetaTrader 5 positions total fetched successfully.\")\n    return OperationResult[int](\n        success=True,\n        data=int(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 positions total fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.copy_rates_from","title":"copy_rates_from","text":"<pre><code>copy_rates_from(\n    symbol: str,\n    timeframe: int,\n    date_from: datetime,\n    count: int,\n) -&gt; OperationResult[list[object]]\n</code></pre> <p>Return bars starting from <code>date_from</code>.</p> Source code in <code>src\\metatrader5_wrapper\\rates\\service.py</code> <pre><code>def copy_rates_from(\n    symbol: str,\n    timeframe: int,\n    date_from: datetime,\n    count: int,\n) -&gt; OperationResult[list[object]]:\n    \"\"\"Return bars starting from ``date_from``.\"\"\"\n    logger.debug(\n        \"Copying rates for %r from %s with timeframe=%r count=%r.\",\n        symbol,\n        date_from,\n        timeframe,\n        count,\n    )\n    result = mt5_rates.copy_rates_from(symbol, timeframe, date_from, count)\n    if not result.success:\n        return OperationResult[list[object]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to copy rates for {symbol!r} from {date_from}.\",\n        )\n    return OperationResult[list[object]](\n        success=True,\n        data=list(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Rates for {symbol!r} copied successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.copy_rates_from_pos","title":"copy_rates_from_pos","text":"<pre><code>copy_rates_from_pos(\n    symbol: str, timeframe: int, start_pos: int, count: int\n) -&gt; OperationResult[list[object]]\n</code></pre> <p>Return bars starting from <code>start_pos</code>.</p> Source code in <code>src\\metatrader5_wrapper\\rates\\service.py</code> <pre><code>def copy_rates_from_pos(\n    symbol: str,\n    timeframe: int,\n    start_pos: int,\n    count: int,\n) -&gt; OperationResult[list[object]]:\n    \"\"\"Return bars starting from ``start_pos``.\"\"\"\n    result = mt5_rates.copy_rates_from_pos(symbol, timeframe, start_pos, count)\n    if not result.success:\n        return OperationResult[list[object]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to copy rates for {symbol!r} from position {start_pos}.\",\n        )\n    return OperationResult[list[object]](\n        success=True,\n        data=list(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Rates for {symbol!r} copied successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.copy_rates_range","title":"copy_rates_range","text":"<pre><code>copy_rates_range(\n    symbol: str,\n    timeframe: int,\n    date_from: datetime,\n    date_to: datetime,\n) -&gt; OperationResult[list[object]]\n</code></pre> <p>Return bars within the given date range.</p> Source code in <code>src\\metatrader5_wrapper\\rates\\service.py</code> <pre><code>def copy_rates_range(\n    symbol: str,\n    timeframe: int,\n    date_from: datetime,\n    date_to: datetime,\n) -&gt; OperationResult[list[object]]:\n    \"\"\"Return bars within the given date range.\"\"\"\n    result = mt5_rates.copy_rates_range(symbol, timeframe, date_from, date_to)\n    if not result.success:\n        return OperationResult[list[object]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to copy rates for {symbol!r} in the given range.\",\n        )\n    return OperationResult[list[object]](\n        success=True,\n        data=list(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Rates for {symbol!r} copied successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.copy_ticks_from","title":"copy_ticks_from","text":"<pre><code>copy_ticks_from(\n    symbol: str, date_from: datetime, count: int, flags: int\n) -&gt; OperationResult[list[object]]\n</code></pre> <p>Return ticks starting from <code>date_from</code>.</p> Source code in <code>src\\metatrader5_wrapper\\rates\\service.py</code> <pre><code>def copy_ticks_from(\n    symbol: str,\n    date_from: datetime,\n    count: int,\n    flags: int,\n) -&gt; OperationResult[list[object]]:\n    \"\"\"Return ticks starting from ``date_from``.\"\"\"\n    result = mt5_rates.copy_ticks_from(symbol, date_from, count, flags)\n    if not result.success:\n        return OperationResult[list[object]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to copy ticks for {symbol!r} from {date_from}.\",\n        )\n    return OperationResult[list[object]](\n        success=True,\n        data=list(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Ticks for {symbol!r} copied successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.copy_ticks_range","title":"copy_ticks_range","text":"<pre><code>copy_ticks_range(\n    symbol: str,\n    date_from: datetime,\n    date_to: datetime,\n    flags: int,\n) -&gt; OperationResult[list[object]]\n</code></pre> <p>Return ticks within the given date range.</p> Source code in <code>src\\metatrader5_wrapper\\rates\\service.py</code> <pre><code>def copy_ticks_range(\n    symbol: str,\n    date_from: datetime,\n    date_to: datetime,\n    flags: int,\n) -&gt; OperationResult[list[object]]:\n    \"\"\"Return ticks within the given date range.\"\"\"\n    result = mt5_rates.copy_ticks_range(symbol, date_from, date_to, flags)\n    if not result.success:\n        return OperationResult[list[object]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to copy ticks for {symbol!r} in the given range.\",\n        )\n    return OperationResult[list[object]](\n        success=True,\n        data=list(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Ticks for {symbol!r} copied successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbol_info","title":"symbol_info","text":"<pre><code>symbol_info(symbol: str) -&gt; OperationResult[SymbolInfo]\n</code></pre> <p>Fetch detailed information for a single trading symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\", \"AAPL\").</p> required <p>Returns:</p> Type Description <code>OperationResult[SymbolInfo]</code> <p>OperationResult[SymbolInfo] with typed symbol metadata on success,</p> <code>OperationResult[SymbolInfo]</code> <p>including name, spread, point value, volume limits, and other</p> <code>OperationResult[SymbolInfo]</code> <p>instrument details.</p> <p>Example::</p> <pre><code>result = symbol_info(\"EURUSD\")\nif result.success and result.data:\n    sym = result.data\n    print(f\"Spread: {sym.spread}\")\n    print(f\"Digits: {sym.digits}\")\n    print(f\"Point: {sym.point}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbol_info(symbol: str) -&gt; OperationResult[SymbolInfo]:\n    \"\"\"Fetch detailed information for a single trading symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\", \"AAPL\").\n\n    Returns:\n        OperationResult[SymbolInfo] with typed symbol metadata on success,\n        including name, spread, point value, volume limits, and other\n        instrument details.\n\n    Example::\n\n        result = symbol_info(\"EURUSD\")\n        if result.success and result.data:\n            sym = result.data\n            print(f\"Spread: {sym.spread}\")\n            print(f\"Digits: {sym.digits}\")\n            print(f\"Point: {sym.point}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 symbol info for %r.\", symbol)\n    result = mt5_symbols.symbol_info(symbol)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch symbol info for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[SymbolInfo](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to fetch symbol info for {symbol!r}.\",\n        )\n\n    logger.info(\"Symbol info for %r fetched successfully.\", symbol)\n    return OperationResult[SymbolInfo](\n        success=True,\n        data=model_from_namedtuple(SymbolInfo, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Symbol info for {symbol!r} fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbol_info_tick","title":"symbol_info_tick","text":"<pre><code>symbol_info_tick(symbol: str) -&gt; OperationResult[Tick]\n</code></pre> <p>Fetch the latest tick data for a symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\").</p> required <p>Returns:</p> Type Description <code>OperationResult[Tick]</code> <p>OperationResult[Tick] with bid, ask, time, and volume data on success.</p> <p>Example::</p> <pre><code>result = symbol_info_tick(\"EURUSD\")\nif result.success and result.data:\n    tick = result.data\n    print(f\"Bid: {tick.bid}\")\n    print(f\"Ask: {tick.ask}\")\n    print(f\"Spread: {tick.ask - tick.bid}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbol_info_tick(symbol: str) -&gt; OperationResult[Tick]:\n    \"\"\"Fetch the latest tick data for a symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\").\n\n    Returns:\n        OperationResult[Tick] with bid, ask, time, and volume data on success.\n\n    Example::\n\n        result = symbol_info_tick(\"EURUSD\")\n        if result.success and result.data:\n            tick = result.data\n            print(f\"Bid: {tick.bid}\")\n            print(f\"Ask: {tick.ask}\")\n            print(f\"Spread: {tick.ask - tick.bid}\")\n    \"\"\"\n    logger.debug(\"Fetching latest MetaTrader 5 tick for %r.\", symbol)\n    result = mt5_symbols.symbol_info_tick(symbol)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch latest tick for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[Tick](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to fetch latest tick for {symbol!r}.\",\n        )\n\n    logger.info(\"Latest tick for %r fetched successfully.\", symbol)\n    return OperationResult[Tick](\n        success=True,\n        data=model_from_namedtuple(Tick, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Latest tick for {symbol!r} fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbol_select","title":"symbol_select","text":"<pre><code>symbol_select(\n    symbol: str, enable: bool = True\n) -&gt; OperationResult[None]\n</code></pre> <p>Add or remove a symbol from the terminal's MarketWatch.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name to select or deselect.</p> required <code>enable</code> <code>bool</code> <p>If <code>True</code> (default), adds the symbol to MarketWatch. If <code>False</code>, removes it.</p> <code>True</code> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating success or failure of the operation.</p> <p>Example::</p> <pre><code># Add EURUSD to MarketWatch\nresult = symbol_select(\"EURUSD\", enable=True)\n\n# Remove from MarketWatch\nresult = symbol_select(\"EURUSD\", enable=False)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbol_select(symbol: str, enable: bool = True) -&gt; OperationResult[None]:\n    \"\"\"Add or remove a symbol from the terminal's MarketWatch.\n\n    Args:\n        symbol: Symbol name to select or deselect.\n        enable: If ``True`` (default), adds the symbol to MarketWatch.\n            If ``False``, removes it.\n\n    Returns:\n        OperationResult[None] indicating success or failure of the operation.\n\n    Example::\n\n        # Add EURUSD to MarketWatch\n        result = symbol_select(\"EURUSD\", enable=True)\n\n        # Remove from MarketWatch\n        result = symbol_select(\"EURUSD\", enable=False)\n    \"\"\"\n    logger.debug(\"Updating MarketWatch visibility for %r to %s.\", symbol, enable)\n    result = mt5_symbols.symbol_select(symbol, enable)\n    if not result.success:\n        logger.warning(\n            \"Unable to update MarketWatch visibility for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[None](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to update MarketWatch visibility for {symbol!r}.\",\n        )\n\n    logger.info(\"MarketWatch visibility updated for %r.\", symbol)\n    return OperationResult[None](\n        success=True,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"MarketWatch visibility updated for {symbol!r}.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbols_get","title":"symbols_get","text":"<pre><code>symbols_get(\n    group: str | None = None,\n) -&gt; OperationResult[list[SymbolInfo]]\n</code></pre> <p>Fetch available trading symbols, optionally filtered by group.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str | None</code> <p>Optional MT5 group filter string (e.g., \"USD\", \"CRYPTO*\"). If <code>None</code>, returns all symbols.</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[SymbolInfo]]</code> <p>OperationResult[list[SymbolInfo]] with a list of typed symbol objects on</p> <code>OperationResult[list[SymbolInfo]]</code> <p>success, or error details on failure.</p> <p>Example::</p> <pre><code># Get all symbols\nresult = symbols_get()\n\n# Get only USD pairs\nresult = symbols_get(group=\"*USD*\")\nif result.success and result.data:\n    for sym in result.data:\n        print(sym.name, sym.spread)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbols_get(group: str | None = None) -&gt; OperationResult[list[SymbolInfo]]:\n    \"\"\"Fetch available trading symbols, optionally filtered by group.\n\n    Args:\n        group: Optional MT5 group filter string (e.g., \"*USD*\", \"CRYPTO*\").\n            If ``None``, returns all symbols.\n\n    Returns:\n        OperationResult[list[SymbolInfo]] with a list of typed symbol objects on\n        success, or error details on failure.\n\n    Example::\n\n        # Get all symbols\n        result = symbols_get()\n\n        # Get only USD pairs\n        result = symbols_get(group=\"*USD*\")\n        if result.success and result.data:\n            for sym in result.data:\n                print(sym.name, sym.spread)\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 symbols with group=%r.\", group)\n    result = mt5_symbols.symbols_get(group=group)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 symbols: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[list[SymbolInfo]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 symbols.\",\n        )\n\n    logger.info(\"MetaTrader 5 symbols fetched successfully.\")\n    return OperationResult[list[SymbolInfo]](\n        success=True,\n        data=models_from_namedtuples(SymbolInfo, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 symbols fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbols_total","title":"symbols_total","text":"<pre><code>symbols_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the total number of tradeable symbols in the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the symbol count on success, or error details</p> <code>OperationResult[int]</code> <p>on failure.</p> <p>Example::</p> <pre><code>result = symbols_total()\nif result.success:\n    print(f\"Terminal has {result.data} symbols\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbols_total() -&gt; OperationResult[int]:\n    \"\"\"Count the total number of tradeable symbols in the terminal.\n\n    Returns:\n        OperationResult[int] with the symbol count on success, or error details\n        on failure.\n\n    Example::\n\n        result = symbols_total()\n        if result.success:\n            print(f\"Terminal has {result.data} symbols\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 symbols total.\")\n    result = mt5_symbols.symbols_total()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 symbols total: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[int](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 symbols total.\",\n        )\n\n    logger.info(\"MetaTrader 5 symbols total fetched successfully.\")\n    return OperationResult[int](\n        success=True,\n        data=int(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 symbols total fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.shutdown","title":"shutdown","text":"<pre><code>shutdown() -&gt; OperationResult[None]\n</code></pre> <p>Cleanly disconnect from the MetaTrader 5 terminal.</p> <p>Closes the active MT5 session and releases resources. Should be called when your application is finished trading or connecting to the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating the shutdown completed successfully.</p> <p>Example::</p> <pre><code>result = shutdown()\nif result.success:\n    print(\"Terminal closed\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\terminal\\service.py</code> <pre><code>def shutdown() -&gt; OperationResult[None]:\n    \"\"\"Cleanly disconnect from the MetaTrader 5 terminal.\n\n    Closes the active MT5 session and releases resources. Should be called when\n    your application is finished trading or connecting to the terminal.\n\n    Returns:\n        OperationResult[None] indicating the shutdown completed successfully.\n\n    Example::\n\n        result = shutdown()\n        if result.success:\n            print(\"Terminal closed\")\n    \"\"\"\n    logger.debug(\"Shutting down MetaTrader 5 terminal.\")\n    mt5_connection.shutdown_terminal()\n    logger.info(\"MetaTrader 5 terminal shutdown completed.\")\n    return OperationResult[None](\n        success=True,\n        message=\"MetaTrader 5 terminal shutdown completed.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.terminal_info","title":"terminal_info","text":"<pre><code>terminal_info() -&gt; OperationResult[TerminalInfo]\n</code></pre> <p>Fetch MetaTrader 5 terminal information and status.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalInfo]</code> <p>OperationResult[TerminalInfo] with terminal metadata including connection</p> <code>OperationResult[TerminalInfo]</code> <p>status, trading permissions, paths, language, and company name.</p> <p>Example::</p> <pre><code>result = terminal_info()\nif result.success and result.data:\n    print(f\"Terminal: {result.data.name}\")\n    print(f\"Build: {result.data.build}\")\n    print(f\"Connected: {result.data.connected}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\terminal\\service.py</code> <pre><code>def terminal_info() -&gt; OperationResult[TerminalInfo]:\n    \"\"\"Fetch MetaTrader 5 terminal information and status.\n\n    Returns:\n        OperationResult[TerminalInfo] with terminal metadata including connection\n        status, trading permissions, paths, language, and company name.\n\n    Example::\n\n        result = terminal_info()\n        if result.success and result.data:\n            print(f\"Terminal: {result.data.name}\")\n            print(f\"Build: {result.data.build}\")\n            print(f\"Connected: {result.data.connected}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 terminal info.\")\n    result = mt5_terminal.terminal_info()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 terminal info: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[TerminalInfo](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 terminal info.\",\n        )\n\n    terminal = model_from_namedtuple(TerminalInfo, result.data)\n    logger.info(\"MetaTrader 5 terminal info fetched successfully.\")\n    return OperationResult[TerminalInfo](\n        success=True,\n        data=terminal,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 terminal info fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.version","title":"version","text":"<pre><code>version() -&gt; OperationResult[TerminalVersion]\n</code></pre> <p>Fetch MetaTrader 5 terminal version and build information.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalVersion]</code> <p>OperationResult[TerminalVersion] with version number, build number, and</p> <code>OperationResult[TerminalVersion]</code> <p>release date.</p> <p>Example::</p> <pre><code>result = version()\nif result.success and result.data:\n    print(f\"Version: {result.data.version}.{result.data.build}\")\n    print(f\"Released: {result.data.release_date}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\terminal\\service.py</code> <pre><code>def version() -&gt; OperationResult[TerminalVersion]:\n    \"\"\"Fetch MetaTrader 5 terminal version and build information.\n\n    Returns:\n        OperationResult[TerminalVersion] with version number, build number, and\n        release date.\n\n    Example::\n\n        result = version()\n        if result.success and result.data:\n            print(f\"Version: {result.data.version}.{result.data.build}\")\n            print(f\"Released: {result.data.release_date}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 terminal version.\")\n    result = mt5_terminal.version()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 terminal version: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[TerminalVersion](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 terminal version.\",\n        )\n\n    version_number, build, release_date = cast(tuple[int, int, str], result.data)\n    logger.info(\"MetaTrader 5 terminal version fetched successfully.\")\n    return OperationResult[TerminalVersion](\n        success=True,\n        data=TerminalVersion(\n            version=version_number,\n            build=build,\n            release_date=release_date,\n        ),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 terminal version fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#client-facade","title":"Client facade","text":""},{"location":"api/#metatrader5_wrapper.client","title":"client","text":""},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client","title":"MetaTrader5Client","text":"<pre><code>MetaTrader5Client(\n    credentials: LoginCredential | None = None,\n)\n</code></pre> <p>Pythonic client facade for typed MetaTrader 5 operations.</p> <p>Provides a clean, context-manager-friendly interface to MT5 connection, account, symbol, and position operations. All errors are captured as typed <code>ConnectionResult</code> or <code>OperationResult</code> objects\u2014no exceptions are raised for normal operation failures.</p> <p>Typical usage:</p> <pre><code>creds = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"password\"),\n    server=\"Broker-Demo\"\n)\nwith MetaTrader5Client(creds) as mt5:\n    account = mt5.account_info()\n    if account.success and account.data:\n        print(account.data.balance)\n</code></pre> <p>Initialize a MetaTrader5 client with optional credentials.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional LoginCredential for automatic connection on context manager entry. If <code>None</code>, credentials must be provided to <code>initialize()</code> or <code>login()</code> later.</p> <code>None</code> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def __init__(self, credentials: LoginCredential | None = None) -&gt; None:\n    \"\"\"Initialize a MetaTrader5 client with optional credentials.\n\n    Args:\n        credentials: Optional LoginCredential for automatic connection on\n            context manager entry. If ``None``, credentials must be provided\n            to ``initialize()`` or ``login()`` later.\n    \"\"\"\n    self.credentials = credentials\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.initialize","title":"initialize","text":"<pre><code>initialize(\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult\n</code></pre> <p>Initialize the terminal and optionally log in.</p> <p>If new credentials are provided, they replace any previously configured credentials for this client instance.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional LoginCredential. If provided, replaces the credentials stored on this client and is used for login.</p> <code>None</code> <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, connection stage, and error details.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def initialize(\n    self,\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult:\n    \"\"\"Initialize the terminal and optionally log in.\n\n    If new credentials are provided, they replace any previously configured\n    credentials for this client instance.\n\n    Args:\n        credentials: Optional LoginCredential. If provided, replaces the\n            credentials stored on this client and is used for login.\n\n    Returns:\n        ConnectionResult with success status, connection stage, and error details.\n    \"\"\"\n    if credentials is not None:\n        self.credentials = credentials\n\n    return initialize_connection(self.credentials)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.connect","title":"connect","text":"<pre><code>connect(\n    credentials: LoginCredential | None = None,\n) -&gt; MetaTrader5Client\n</code></pre> <p>Initialize the terminal and return the client on success.</p> <p>This is a convenience alias for application code that prefers a fluent, fail-fast startup path.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def connect(\n    self,\n    credentials: LoginCredential | None = None,\n) -&gt; \"MetaTrader5Client\":\n    \"\"\"Initialize the terminal and return the client on success.\n\n    This is a convenience alias for application code that prefers a fluent,\n    fail-fast startup path.\n    \"\"\"\n    result = self.initialize(credentials)\n    result.expect(\"Unable to connect to MetaTrader 5\")\n    return self\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.account_info","title":"account_info","text":"<pre><code>account_info() -&gt; OperationResult[AccountInfo]\n</code></pre> <p>Fetch current account details as a typed model.</p> <p>Requires an active, logged-in MT5 session.</p> <p>Returns:</p> Type Description <code>OperationResult[AccountInfo]</code> <p>OperationResult[AccountInfo] with typed account data on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def account_info(self) -&gt; OperationResult[AccountInfo]:\n    \"\"\"Fetch current account details as a typed model.\n\n    Requires an active, logged-in MT5 session.\n\n    Returns:\n        OperationResult[AccountInfo] with typed account data on success.\n    \"\"\"\n    return get_account_info()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.account","title":"account","text":"<pre><code>account() -&gt; AccountInfo\n</code></pre> <p>Return current account details or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def account(self) -&gt; AccountInfo:\n    \"\"\"Return current account details or raise on failure.\"\"\"\n    return self.account_info().expect(\"Unable to fetch account info\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.login","title":"login","text":"<pre><code>login(\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult\n</code></pre> <p>Log in to an MT5 account.</p> <p>Use this to defer login until after the terminal is initialized, or to switch between accounts.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional LoginCredential. If provided, replaces the credentials stored on this client.</p> <code>None</code> <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, stage, and error details.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no credentials have been configured on this client and none are provided here.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def login(self, credentials: LoginCredential | None = None) -&gt; ConnectionResult:\n    \"\"\"Log in to an MT5 account.\n\n    Use this to defer login until after the terminal is initialized, or to\n    switch between accounts.\n\n    Args:\n        credentials: Optional LoginCredential. If provided, replaces the\n            credentials stored on this client.\n\n    Returns:\n        ConnectionResult with success status, stage, and error details.\n\n    Raises:\n        ValueError: If no credentials have been configured on this client\n            and none are provided here.\n    \"\"\"\n    if credentials is not None:\n        self.credentials = credentials\n    if self.credentials is None:\n        raise ValueError(\"credentials are required to log in\")\n\n    return login_account(self.credentials)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.terminal_info","title":"terminal_info","text":"<pre><code>terminal_info() -&gt; OperationResult[TerminalInfo]\n</code></pre> <p>Fetch terminal information and status.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalInfo]</code> <p>OperationResult[TerminalInfo] with terminal metadata on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def terminal_info(self) -&gt; OperationResult[TerminalInfo]:\n    \"\"\"Fetch terminal information and status.\n\n    Returns:\n        OperationResult[TerminalInfo] with terminal metadata on success.\n    \"\"\"\n    return get_terminal_info()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.terminal","title":"terminal","text":"<pre><code>terminal() -&gt; TerminalInfo\n</code></pre> <p>Return terminal details or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def terminal(self) -&gt; TerminalInfo:\n    \"\"\"Return terminal details or raise on failure.\"\"\"\n    return self.terminal_info().expect(\"Unable to fetch terminal info\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.symbols_total","title":"symbols_total","text":"<pre><code>symbols_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the total number of tradeable symbols in the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the symbol count on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbols_total(self) -&gt; OperationResult[int]:\n    \"\"\"Count the total number of tradeable symbols in the terminal.\n\n    Returns:\n        OperationResult[int] with the symbol count on success.\n    \"\"\"\n    return get_symbols_total()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.symbols_get","title":"symbols_get","text":"<pre><code>symbols_get(\n    group: str | None = None,\n) -&gt; OperationResult[list[SymbolInfo]]\n</code></pre> <p>Fetch available trading symbols, optionally filtered by group.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str | None</code> <p>Optional MT5 group filter (e.g., \"USD\", \"CRYPTO*\").</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[SymbolInfo]]</code> <p>OperationResult[list[SymbolInfo]] with symbol list on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbols_get(\n    self, group: str | None = None\n) -&gt; OperationResult[list[SymbolInfo]]:\n    \"\"\"Fetch available trading symbols, optionally filtered by group.\n\n    Args:\n        group: Optional MT5 group filter (e.g., \"*USD*\", \"CRYPTO*\").\n\n    Returns:\n        OperationResult[list[SymbolInfo]] with symbol list on success.\n    \"\"\"\n    return get_symbols(group=group)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.symbols","title":"symbols","text":"<pre><code>symbols(group: str | None = None) -&gt; list[SymbolInfo]\n</code></pre> <p>Return trading symbols or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbols(self, group: str | None = None) -&gt; list[SymbolInfo]:\n    \"\"\"Return trading symbols or raise on failure.\"\"\"\n    return self.symbols_get(group=group).expect(\"Unable to fetch symbols\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.symbol_info","title":"symbol_info","text":"<pre><code>symbol_info(symbol: str) -&gt; OperationResult[SymbolInfo]\n</code></pre> <p>Fetch detailed information for a single trading symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\").</p> required <p>Returns:</p> Type Description <code>OperationResult[SymbolInfo]</code> <p>OperationResult[SymbolInfo] with typed symbol metadata on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol_info(self, symbol: str) -&gt; OperationResult[SymbolInfo]:\n    \"\"\"Fetch detailed information for a single trading symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\").\n\n    Returns:\n        OperationResult[SymbolInfo] with typed symbol metadata on success.\n    \"\"\"\n    return get_symbol_info(symbol)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.symbol","title":"symbol","text":"<pre><code>symbol(symbol: str) -&gt; SymbolInfo\n</code></pre> <p>Return one trading symbol or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol(self, symbol: str) -&gt; SymbolInfo:\n    \"\"\"Return one trading symbol or raise on failure.\"\"\"\n    return self.symbol_info(symbol).expect(\n        f\"Unable to fetch symbol info for {symbol!r}\"\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.symbol_info_tick","title":"symbol_info_tick","text":"<pre><code>symbol_info_tick(symbol: str) -&gt; OperationResult[Tick]\n</code></pre> <p>Fetch the latest tick data for a symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\").</p> required <p>Returns:</p> Type Description <code>OperationResult[Tick]</code> <p>OperationResult[Tick] with bid, ask, time, and volume on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol_info_tick(self, symbol: str) -&gt; OperationResult[Tick]:\n    \"\"\"Fetch the latest tick data for a symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\").\n\n    Returns:\n        OperationResult[Tick] with bid, ask, time, and volume on success.\n    \"\"\"\n    return get_symbol_info_tick(symbol)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.tick","title":"tick","text":"<pre><code>tick(symbol: str) -&gt; Tick\n</code></pre> <p>Return the latest tick for <code>symbol</code> or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def tick(self, symbol: str) -&gt; Tick:\n    \"\"\"Return the latest tick for ``symbol`` or raise on failure.\"\"\"\n    return self.symbol_info_tick(symbol).expect(\n        f\"Unable to fetch latest tick for {symbol!r}\"\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.symbol_select","title":"symbol_select","text":"<pre><code>symbol_select(\n    symbol: str, enable: bool = True\n) -&gt; OperationResult[None]\n</code></pre> <p>Add or remove a symbol from the terminal's MarketWatch.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name.</p> required <code>enable</code> <code>bool</code> <p>If <code>True</code> (default), adds to MarketWatch. If <code>False</code>, removes.</p> <code>True</code> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating success or failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def symbol_select(self, symbol: str, enable: bool = True) -&gt; OperationResult[None]:\n    \"\"\"Add or remove a symbol from the terminal's MarketWatch.\n\n    Args:\n        symbol: Symbol name.\n        enable: If ``True`` (default), adds to MarketWatch. If ``False``, removes.\n\n    Returns:\n        OperationResult[None] indicating success or failure.\n    \"\"\"\n    return select_symbol(symbol, enable=enable)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.positions_total","title":"positions_total","text":"<pre><code>positions_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the number of currently open positions.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the position count on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def positions_total(self) -&gt; OperationResult[int]:\n    \"\"\"Count the number of currently open positions.\n\n    Returns:\n        OperationResult[int] with the position count on success.\n    \"\"\"\n    return get_positions_total()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.positions_get","title":"positions_get","text":"<pre><code>positions_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]\n</code></pre> <p>Fetch open positions, optionally filtered by symbol, group, or ticket.</p> <p>The filter is applied in priority order: symbol, then group, then ticket.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str | None</code> <p>Optional symbol name to filter positions.</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Optional group filter using MT5 wildcards.</p> <code>None</code> <code>ticket</code> <code>int | None</code> <p>Optional position ticket number for exact lookup.</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[Position]]</code> <p>OperationResult[list[Position]] with typed positions on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def positions_get(\n    self,\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]:\n    \"\"\"Fetch open positions, optionally filtered by symbol, group, or ticket.\n\n    The filter is applied in priority order: symbol, then group, then ticket.\n\n    Args:\n        symbol: Optional symbol name to filter positions.\n        group: Optional group filter using MT5 wildcards.\n        ticket: Optional position ticket number for exact lookup.\n\n    Returns:\n        OperationResult[list[Position]] with typed positions on success.\n    \"\"\"\n    return get_positions(symbol=symbol, group=group, ticket=ticket)\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.positions","title":"positions","text":"<pre><code>positions(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; list[Position]\n</code></pre> <p>Return positions or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def positions(\n    self,\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; list[Position]:\n    \"\"\"Return positions or raise on failure.\"\"\"\n    return self.positions_get(\n        symbol=symbol,\n        group=group,\n        ticket=ticket,\n    ).expect(\"Unable to fetch positions\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.version","title":"version","text":"<pre><code>version() -&gt; OperationResult[TerminalVersion]\n</code></pre> <p>Fetch terminal version and build information.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalVersion]</code> <p>OperationResult[TerminalVersion] with version details on success.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def version(self) -&gt; OperationResult[TerminalVersion]:\n    \"\"\"Fetch terminal version and build information.\n\n    Returns:\n        OperationResult[TerminalVersion] with version details on success.\n    \"\"\"\n    return get_version()\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.terminal_version","title":"terminal_version","text":"<pre><code>terminal_version() -&gt; TerminalVersion\n</code></pre> <p>Return terminal version details or raise on failure.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def terminal_version(self) -&gt; TerminalVersion:\n    \"\"\"Return terminal version details or raise on failure.\"\"\"\n    return self.version().expect(\"Unable to fetch terminal version\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.client.MetaTrader5Client.shutdown","title":"shutdown","text":"<pre><code>shutdown() -&gt; OperationResult[None]\n</code></pre> <p>Cleanly disconnect from the MetaTrader 5 terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating shutdown completed.</p> Source code in <code>src\\metatrader5_wrapper\\client.py</code> <pre><code>def shutdown(self) -&gt; OperationResult[None]:\n    \"\"\"Cleanly disconnect from the MetaTrader 5 terminal.\n\n    Returns:\n        OperationResult[None] indicating shutdown completed.\n    \"\"\"\n    return shutdown_terminal()\n</code></pre>"},{"location":"api/#connection","title":"Connection","text":""},{"location":"api/#metatrader5_wrapper.connection","title":"connection","text":""},{"location":"api/#metatrader5_wrapper.connection.ConnectionResult","title":"ConnectionResult","text":"<p>               Bases: <code>BaseModel</code></p> <p>Structured result for an MT5 connection attempt.</p>"},{"location":"api/#metatrader5_wrapper.connection.ConnectionResult.failed","title":"failed  <code>property</code>","text":"<pre><code>failed: bool\n</code></pre> <p>Return <code>True</code> when the connection operation did not succeed.</p>"},{"location":"api/#metatrader5_wrapper.connection.ConnectionResult.unwrap","title":"unwrap","text":"<pre><code>unwrap() -&gt; None\n</code></pre> <p>Return <code>None</code> on success or raise <code>RuntimeError</code> on failure.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\models.py</code> <pre><code>def unwrap(self) -&gt; None:\n    \"\"\"Return ``None`` on success or raise ``RuntimeError`` on failure.\"\"\"\n    if self.success:\n        return\n    raise RuntimeError(self.describe_error())\n</code></pre>"},{"location":"api/#metatrader5_wrapper.connection.ConnectionResult.expect","title":"expect","text":"<pre><code>expect(message: str) -&gt; None\n</code></pre> <p>Return <code>None</code> on success or raise <code>RuntimeError</code> with context.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\models.py</code> <pre><code>def expect(self, message: str) -&gt; None:\n    \"\"\"Return ``None`` on success or raise ``RuntimeError`` with context.\"\"\"\n    if self.success:\n        return\n    raise RuntimeError(f\"{message}: {self.describe_error()}\")\n</code></pre>"},{"location":"api/#metatrader5_wrapper.connection.ConnectionResult.describe_error","title":"describe_error","text":"<pre><code>describe_error() -&gt; str\n</code></pre> <p>Return a human-readable summary of the connection failure.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\models.py</code> <pre><code>def describe_error(self) -&gt; str:\n    \"\"\"Return a human-readable summary of the connection failure.\"\"\"\n    base_message = self.message or \"MetaTrader 5 connection failed\"\n    return (\n        f\"{base_message} during {self.stage} [{self.error_code}]\"\n        f\" {self.error_message}\"\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.connection.ConnectionStage","title":"ConnectionStage","text":"<p>               Bases: <code>StrEnum</code></p> <p>Connection operation stage associated with the result.</p>"},{"location":"api/#metatrader5_wrapper.connection.LoginCredential","title":"LoginCredential","text":"<p>               Bases: <code>BaseModel</code></p> <p>Credentials and terminal options used to connect to an MT5 account.</p>"},{"location":"api/#metatrader5_wrapper.connection.LoginCredential.timeout_ms","title":"timeout_ms  <code>property</code>","text":"<pre><code>timeout_ms: int\n</code></pre> <p>Return the configured timeout in milliseconds for the MT5 API.</p>"},{"location":"api/#metatrader5_wrapper.connection.initialize","title":"initialize","text":"<pre><code>initialize(\n    credentials: LoginCredential | None = None,\n) -&gt; ConnectionResult\n</code></pre> <p>Initialize the MetaTrader 5 terminal and optionally log in.</p> <p>When called without credentials, initializes the terminal for use with an existing MT5 session or manual login later. When called with credentials, performs both initialization and account login as a single operation.</p> <p>The official MT5 module stores errors globally in <code>last_error()</code>. This wrapper captures that error state immediately after each call and returns it as part of the typed <code>ConnectionResult</code>, eliminating the need to check mutable global state.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential | None</code> <p>Optional <code>LoginCredential</code> containing account number, password, server name, and connection timeout. If <code>None</code>, the terminal initializes without automatic login.</p> <code>None</code> <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, stage (initialize or login), raw MT5</p> <code>ConnectionResult</code> <p>error code and message, and a human-readable status message.</p> Example <p>Initialize without credentials::</p> <pre><code>result = initialize()\nif result.success:\n    print(\"Terminal ready\")\n</code></pre> <p>Initialize with credentials::</p> <pre><code>creds = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"mypassword\"),\n    server=\"Broker-Demo\"\n)\nresult = initialize(creds)\nif result.failed:\n    print(result.stage, result.error_code)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\connection\\service.py</code> <pre><code>def initialize(credentials: LoginCredential | None = None) -&gt; ConnectionResult:\n    \"\"\"Initialize the MetaTrader 5 terminal and optionally log in.\n\n    When called without credentials, initializes the terminal for use with an\n    existing MT5 session or manual login later. When called with credentials,\n    performs both initialization and account login as a single operation.\n\n    The official MT5 module stores errors globally in ``last_error()``. This\n    wrapper captures that error state immediately after each call and returns it\n    as part of the typed ``ConnectionResult``, eliminating the need to check\n    mutable global state.\n\n    Args:\n        credentials: Optional ``LoginCredential`` containing account number, password,\n            server name, and connection timeout. If ``None``, the terminal initializes\n            without automatic login.\n\n    Returns:\n        ConnectionResult with success status, stage (initialize or login), raw MT5\n        error code and message, and a human-readable status message.\n\n    Example:\n        Initialize without credentials::\n\n            result = initialize()\n            if result.success:\n                print(\"Terminal ready\")\n\n        Initialize with credentials::\n\n            creds = LoginCredential(\n                login=12345678,\n                password=SecretStr(\"mypassword\"),\n                server=\"Broker-Demo\"\n            )\n            result = initialize(creds)\n            if result.failed:\n                print(result.stage, result.error_code)\n    \"\"\"\n    logger.debug(\"Initializing MetaTrader 5 terminal.\")\n    init_result = mt5_connection.initialize_terminal(\n        terminal_path=credentials.terminal_path if credentials else None,\n        timeout_ms=credentials.timeout_ms if credentials else 60_000,\n        portable=credentials.portable if credentials else False,\n    )\n    if not init_result.success:\n        logger.warning(\n            \"MetaTrader 5 terminal initialization failed: [%s] %s\",\n            init_result.error.code,\n            init_result.error.message,\n        )\n        return _to_connection_result(\n            init_result,\n            stage=ConnectionStage.INITIALIZE,\n            message=\"Unable to initialize the MetaTrader 5 terminal.\",\n        )\n\n    if credentials is None:\n        logger.info(\"MetaTrader 5 terminal initialized successfully.\")\n        return _to_connection_result(\n            init_result,\n            stage=ConnectionStage.INITIALIZE,\n            message=\"MetaTrader 5 terminal initialized successfully.\",\n        )\n\n    logger.debug(\"Logging in to MetaTrader 5 account %s.\", credentials.login)\n    login_result = mt5_connection.login_account(\n        login=credentials.login,\n        password=credentials.password.get_secret_value(),\n        server=credentials.server,\n        timeout_ms=credentials.timeout_ms,\n    )\n    if not login_result.success:\n        logger.warning(\n            \"MetaTrader 5 account login failed for %s: [%s] %s\",\n            credentials.login,\n            login_result.error.code,\n            login_result.error.message,\n        )\n        return _to_connection_result(\n            login_result,\n            stage=ConnectionStage.LOGIN,\n            message=\"Unable to log in to the MetaTrader 5 account.\",\n        )\n\n    logger.info(\"MetaTrader 5 account %s connected successfully.\", credentials.login)\n    return _to_connection_result(\n        login_result,\n        stage=ConnectionStage.LOGIN,\n        message=\"MetaTrader 5 account connected successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.connection.login","title":"login","text":"<pre><code>login(credentials: LoginCredential) -&gt; ConnectionResult\n</code></pre> <p>Log in to an MT5 account after the terminal has been initialized.</p> <p>Use this function when you want to defer login until after the terminal is already running, or when you need to switch between accounts in the same session.</p> <p>Parameters:</p> Name Type Description Default <code>credentials</code> <code>LoginCredential</code> <p>LoginCredential containing account number, password, server, and connection timeout.</p> required <p>Returns:</p> Type Description <code>ConnectionResult</code> <p>ConnectionResult with success status, stage set to LOGIN, raw MT5 error</p> <code>ConnectionResult</code> <p>code and message, and a descriptive status message.</p> Source code in <code>src\\metatrader5_wrapper\\connection\\service.py</code> <pre><code>def login(credentials: LoginCredential) -&gt; ConnectionResult:\n    \"\"\"Log in to an MT5 account after the terminal has been initialized.\n\n    Use this function when you want to defer login until after the terminal\n    is already running, or when you need to switch between accounts in the\n    same session.\n\n    Args:\n        credentials: LoginCredential containing account number, password, server,\n            and connection timeout.\n\n    Returns:\n        ConnectionResult with success status, stage set to LOGIN, raw MT5 error\n        code and message, and a descriptive status message.\n    \"\"\"\n    logger.debug(\"Logging in to MetaTrader 5 account %s.\", credentials.login)\n    login_result = mt5_connection.login_account(\n        login=credentials.login,\n        password=credentials.password.get_secret_value(),\n        server=credentials.server,\n        timeout_ms=credentials.timeout_ms,\n    )\n    if not login_result.success:\n        logger.warning(\n            \"MetaTrader 5 account login failed for %s: [%s] %s\",\n            credentials.login,\n            login_result.error.code,\n            login_result.error.message,\n        )\n        return _to_connection_result(\n            login_result,\n            stage=ConnectionStage.LOGIN,\n            message=\"Unable to log in to the MetaTrader 5 account.\",\n        )\n\n    logger.info(\"MetaTrader 5 account %s connected successfully.\", credentials.login)\n    return _to_connection_result(\n        login_result,\n        stage=ConnectionStage.LOGIN,\n        message=\"MetaTrader 5 account connected successfully.\",\n    )\n</code></pre>"},{"location":"api/#account","title":"Account","text":""},{"location":"api/#metatrader5_wrapper.account","title":"account","text":""},{"location":"api/#metatrader5_wrapper.account.AccountInfo","title":"AccountInfo","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed account details returned by <code>MetaTrader5.account_info()</code>.</p>"},{"location":"api/#metatrader5_wrapper.account.account_info","title":"account_info","text":"<pre><code>account_info() -&gt; OperationResult[AccountInfo]\n</code></pre> <p>Fetch current account details as a typed model.</p> <p>Requires an active, logged-in MT5 session. Reads account balance, equity, margin, leverage, trading permissions, and account metadata from the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[AccountInfo]</code> <p>OperationResult[AccountInfo] with success status, typed AccountInfo data</p> <code>OperationResult[AccountInfo]</code> <p>on success, raw MT5 error code and message on failure, and a status message.</p> <p>Example::</p> <pre><code>result = account_info()\nif result.success and result.data:\n    print(f\"Balance: {result.data.balance}\")\n    print(f\"Equity: {result.data.equity}\")\n    print(f\"Margin: {result.data.margin}\")\nelse:\n    print(result.error_code, result.error_message)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\account\\service.py</code> <pre><code>def account_info() -&gt; OperationResult[AccountInfo]:\n    \"\"\"Fetch current account details as a typed model.\n\n    Requires an active, logged-in MT5 session. Reads account balance, equity,\n    margin, leverage, trading permissions, and account metadata from the\n    terminal.\n\n    Returns:\n        OperationResult[AccountInfo] with success status, typed AccountInfo data\n        on success, raw MT5 error code and message on failure, and a status message.\n\n    Example::\n\n        result = account_info()\n        if result.success and result.data:\n            print(f\"Balance: {result.data.balance}\")\n            print(f\"Equity: {result.data.equity}\")\n            print(f\"Margin: {result.data.margin}\")\n        else:\n            print(result.error_code, result.error_message)\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 account info.\")\n    result = mt5_account.account_info()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 account info: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[AccountInfo](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 account info.\",\n        )\n\n    account = model_from_namedtuple(AccountInfo, result.data)\n    logger.info(\"MetaTrader 5 account info fetched successfully.\")\n    return OperationResult[AccountInfo](\n        success=True,\n        data=account,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 account info fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#symbols","title":"Symbols","text":""},{"location":"api/#metatrader5_wrapper.symbols","title":"symbols","text":""},{"location":"api/#metatrader5_wrapper.symbols.SymbolInfo","title":"SymbolInfo","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed symbol details returned by <code>MetaTrader5.symbol_info()</code>.</p>"},{"location":"api/#metatrader5_wrapper.symbols.Tick","title":"Tick","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed latest-tick payload returned by <code>MetaTrader5.symbol_info_tick()</code>.</p>"},{"location":"api/#metatrader5_wrapper.symbols.symbol_info","title":"symbol_info","text":"<pre><code>symbol_info(symbol: str) -&gt; OperationResult[SymbolInfo]\n</code></pre> <p>Fetch detailed information for a single trading symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\", \"AAPL\").</p> required <p>Returns:</p> Type Description <code>OperationResult[SymbolInfo]</code> <p>OperationResult[SymbolInfo] with typed symbol metadata on success,</p> <code>OperationResult[SymbolInfo]</code> <p>including name, spread, point value, volume limits, and other</p> <code>OperationResult[SymbolInfo]</code> <p>instrument details.</p> <p>Example::</p> <pre><code>result = symbol_info(\"EURUSD\")\nif result.success and result.data:\n    sym = result.data\n    print(f\"Spread: {sym.spread}\")\n    print(f\"Digits: {sym.digits}\")\n    print(f\"Point: {sym.point}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbol_info(symbol: str) -&gt; OperationResult[SymbolInfo]:\n    \"\"\"Fetch detailed information for a single trading symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\", \"AAPL\").\n\n    Returns:\n        OperationResult[SymbolInfo] with typed symbol metadata on success,\n        including name, spread, point value, volume limits, and other\n        instrument details.\n\n    Example::\n\n        result = symbol_info(\"EURUSD\")\n        if result.success and result.data:\n            sym = result.data\n            print(f\"Spread: {sym.spread}\")\n            print(f\"Digits: {sym.digits}\")\n            print(f\"Point: {sym.point}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 symbol info for %r.\", symbol)\n    result = mt5_symbols.symbol_info(symbol)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch symbol info for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[SymbolInfo](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to fetch symbol info for {symbol!r}.\",\n        )\n\n    logger.info(\"Symbol info for %r fetched successfully.\", symbol)\n    return OperationResult[SymbolInfo](\n        success=True,\n        data=model_from_namedtuple(SymbolInfo, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Symbol info for {symbol!r} fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbols.symbol_info_tick","title":"symbol_info_tick","text":"<pre><code>symbol_info_tick(symbol: str) -&gt; OperationResult[Tick]\n</code></pre> <p>Fetch the latest tick data for a symbol.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name (e.g., \"EURUSD\").</p> required <p>Returns:</p> Type Description <code>OperationResult[Tick]</code> <p>OperationResult[Tick] with bid, ask, time, and volume data on success.</p> <p>Example::</p> <pre><code>result = symbol_info_tick(\"EURUSD\")\nif result.success and result.data:\n    tick = result.data\n    print(f\"Bid: {tick.bid}\")\n    print(f\"Ask: {tick.ask}\")\n    print(f\"Spread: {tick.ask - tick.bid}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbol_info_tick(symbol: str) -&gt; OperationResult[Tick]:\n    \"\"\"Fetch the latest tick data for a symbol.\n\n    Args:\n        symbol: Symbol name (e.g., \"EURUSD\").\n\n    Returns:\n        OperationResult[Tick] with bid, ask, time, and volume data on success.\n\n    Example::\n\n        result = symbol_info_tick(\"EURUSD\")\n        if result.success and result.data:\n            tick = result.data\n            print(f\"Bid: {tick.bid}\")\n            print(f\"Ask: {tick.ask}\")\n            print(f\"Spread: {tick.ask - tick.bid}\")\n    \"\"\"\n    logger.debug(\"Fetching latest MetaTrader 5 tick for %r.\", symbol)\n    result = mt5_symbols.symbol_info_tick(symbol)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch latest tick for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[Tick](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to fetch latest tick for {symbol!r}.\",\n        )\n\n    logger.info(\"Latest tick for %r fetched successfully.\", symbol)\n    return OperationResult[Tick](\n        success=True,\n        data=model_from_namedtuple(Tick, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"Latest tick for {symbol!r} fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbols.symbol_select","title":"symbol_select","text":"<pre><code>symbol_select(\n    symbol: str, enable: bool = True\n) -&gt; OperationResult[None]\n</code></pre> <p>Add or remove a symbol from the terminal's MarketWatch.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str</code> <p>Symbol name to select or deselect.</p> required <code>enable</code> <code>bool</code> <p>If <code>True</code> (default), adds the symbol to MarketWatch. If <code>False</code>, removes it.</p> <code>True</code> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating success or failure of the operation.</p> <p>Example::</p> <pre><code># Add EURUSD to MarketWatch\nresult = symbol_select(\"EURUSD\", enable=True)\n\n# Remove from MarketWatch\nresult = symbol_select(\"EURUSD\", enable=False)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbol_select(symbol: str, enable: bool = True) -&gt; OperationResult[None]:\n    \"\"\"Add or remove a symbol from the terminal's MarketWatch.\n\n    Args:\n        symbol: Symbol name to select or deselect.\n        enable: If ``True`` (default), adds the symbol to MarketWatch.\n            If ``False``, removes it.\n\n    Returns:\n        OperationResult[None] indicating success or failure of the operation.\n\n    Example::\n\n        # Add EURUSD to MarketWatch\n        result = symbol_select(\"EURUSD\", enable=True)\n\n        # Remove from MarketWatch\n        result = symbol_select(\"EURUSD\", enable=False)\n    \"\"\"\n    logger.debug(\"Updating MarketWatch visibility for %r to %s.\", symbol, enable)\n    result = mt5_symbols.symbol_select(symbol, enable)\n    if not result.success:\n        logger.warning(\n            \"Unable to update MarketWatch visibility for %r: [%s] %s\",\n            symbol,\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[None](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=f\"Unable to update MarketWatch visibility for {symbol!r}.\",\n        )\n\n    logger.info(\"MarketWatch visibility updated for %r.\", symbol)\n    return OperationResult[None](\n        success=True,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=f\"MarketWatch visibility updated for {symbol!r}.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbols.symbols_get","title":"symbols_get","text":"<pre><code>symbols_get(\n    group: str | None = None,\n) -&gt; OperationResult[list[SymbolInfo]]\n</code></pre> <p>Fetch available trading symbols, optionally filtered by group.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str | None</code> <p>Optional MT5 group filter string (e.g., \"USD\", \"CRYPTO*\"). If <code>None</code>, returns all symbols.</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[SymbolInfo]]</code> <p>OperationResult[list[SymbolInfo]] with a list of typed symbol objects on</p> <code>OperationResult[list[SymbolInfo]]</code> <p>success, or error details on failure.</p> <p>Example::</p> <pre><code># Get all symbols\nresult = symbols_get()\n\n# Get only USD pairs\nresult = symbols_get(group=\"*USD*\")\nif result.success and result.data:\n    for sym in result.data:\n        print(sym.name, sym.spread)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbols_get(group: str | None = None) -&gt; OperationResult[list[SymbolInfo]]:\n    \"\"\"Fetch available trading symbols, optionally filtered by group.\n\n    Args:\n        group: Optional MT5 group filter string (e.g., \"*USD*\", \"CRYPTO*\").\n            If ``None``, returns all symbols.\n\n    Returns:\n        OperationResult[list[SymbolInfo]] with a list of typed symbol objects on\n        success, or error details on failure.\n\n    Example::\n\n        # Get all symbols\n        result = symbols_get()\n\n        # Get only USD pairs\n        result = symbols_get(group=\"*USD*\")\n        if result.success and result.data:\n            for sym in result.data:\n                print(sym.name, sym.spread)\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 symbols with group=%r.\", group)\n    result = mt5_symbols.symbols_get(group=group)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 symbols: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[list[SymbolInfo]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 symbols.\",\n        )\n\n    logger.info(\"MetaTrader 5 symbols fetched successfully.\")\n    return OperationResult[list[SymbolInfo]](\n        success=True,\n        data=models_from_namedtuples(SymbolInfo, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 symbols fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.symbols.symbols_total","title":"symbols_total","text":"<pre><code>symbols_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the total number of tradeable symbols in the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the symbol count on success, or error details</p> <code>OperationResult[int]</code> <p>on failure.</p> <p>Example::</p> <pre><code>result = symbols_total()\nif result.success:\n    print(f\"Terminal has {result.data} symbols\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\symbols\\service.py</code> <pre><code>def symbols_total() -&gt; OperationResult[int]:\n    \"\"\"Count the total number of tradeable symbols in the terminal.\n\n    Returns:\n        OperationResult[int] with the symbol count on success, or error details\n        on failure.\n\n    Example::\n\n        result = symbols_total()\n        if result.success:\n            print(f\"Terminal has {result.data} symbols\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 symbols total.\")\n    result = mt5_symbols.symbols_total()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 symbols total: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[int](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 symbols total.\",\n        )\n\n    logger.info(\"MetaTrader 5 symbols total fetched successfully.\")\n    return OperationResult[int](\n        success=True,\n        data=int(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 symbols total fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#positions","title":"Positions","text":""},{"location":"api/#metatrader5_wrapper.positions","title":"positions","text":""},{"location":"api/#metatrader5_wrapper.positions.Position","title":"Position","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed position payload returned by <code>MetaTrader5.positions_get()</code>.</p>"},{"location":"api/#metatrader5_wrapper.positions.positions_get","title":"positions_get","text":"<pre><code>positions_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]\n</code></pre> <p>Fetch open positions, optionally filtered by symbol, group, or ticket.</p> <p>The filter is applied in priority order: symbol is checked first, then group, then ticket. Only one filter is used even if multiple are provided.</p> <p>Parameters:</p> Name Type Description Default <code>symbol</code> <code>str | None</code> <p>Optional symbol name to filter positions (e.g., \"EURUSD\").</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Optional group filter using MT5 wildcards (e.g., \"USD\").</p> <code>None</code> <code>ticket</code> <code>int | None</code> <p>Optional position ticket number for exact lookup.</p> <code>None</code> <p>Returns:</p> Type Description <code>OperationResult[list[Position]]</code> <p>OperationResult[list[Position]] with typed position objects on success,</p> <code>OperationResult[list[Position]]</code> <p>or error details on failure.</p> <p>Example::</p> <pre><code># Get all open positions\nresult = positions_get()\n\n# Get positions for EURUSD only\nresult = positions_get(symbol=\"EURUSD\")\nif result.success and result.data:\n    for pos in result.data:\n        print(f\"Ticket: {pos.ticket}, Volume: {pos.volume}\")\n\n# Get a specific position by ticket\nresult = positions_get(ticket=12345)\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\positions\\service.py</code> <pre><code>def positions_get(\n    *,\n    symbol: str | None = None,\n    group: str | None = None,\n    ticket: int | None = None,\n) -&gt; OperationResult[list[Position]]:\n    \"\"\"Fetch open positions, optionally filtered by symbol, group, or ticket.\n\n    The filter is applied in priority order: symbol is checked first, then group,\n    then ticket. Only one filter is used even if multiple are provided.\n\n    Args:\n        symbol: Optional symbol name to filter positions (e.g., \"EURUSD\").\n        group: Optional group filter using MT5 wildcards (e.g., \"*USD*\").\n        ticket: Optional position ticket number for exact lookup.\n\n    Returns:\n        OperationResult[list[Position]] with typed position objects on success,\n        or error details on failure.\n\n    Example::\n\n        # Get all open positions\n        result = positions_get()\n\n        # Get positions for EURUSD only\n        result = positions_get(symbol=\"EURUSD\")\n        if result.success and result.data:\n            for pos in result.data:\n                print(f\"Ticket: {pos.ticket}, Volume: {pos.volume}\")\n\n        # Get a specific position by ticket\n        result = positions_get(ticket=12345)\n    \"\"\"\n    logger.debug(\n        \"Fetching MetaTrader 5 positions with symbol=%r group=%r ticket=%r.\",\n        symbol,\n        group,\n        ticket,\n    )\n    result = mt5_positions.positions_get(symbol=symbol, group=group, ticket=ticket)\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 positions: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[list[Position]](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 positions.\",\n        )\n\n    logger.info(\"MetaTrader 5 positions fetched successfully.\")\n    return OperationResult[list[Position]](\n        success=True,\n        data=models_from_namedtuples(Position, result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 positions fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.positions.positions_total","title":"positions_total","text":"<pre><code>positions_total() -&gt; OperationResult[int]\n</code></pre> <p>Count the number of currently open positions.</p> <p>Returns:</p> Type Description <code>OperationResult[int]</code> <p>OperationResult[int] with the position count on success.</p> <p>Example::</p> <pre><code>result = positions_total()\nif result.success:\n    print(f\"Open positions: {result.data}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\positions\\service.py</code> <pre><code>def positions_total() -&gt; OperationResult[int]:\n    \"\"\"Count the number of currently open positions.\n\n    Returns:\n        OperationResult[int] with the position count on success.\n\n    Example::\n\n        result = positions_total()\n        if result.success:\n            print(f\"Open positions: {result.data}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 positions total.\")\n    result = mt5_positions.positions_total()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 positions total: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[int](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 positions total.\",\n        )\n\n    logger.info(\"MetaTrader 5 positions total fetched successfully.\")\n    return OperationResult[int](\n        success=True,\n        data=int(result.data),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 positions total fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#terminal","title":"Terminal","text":""},{"location":"api/#metatrader5_wrapper.terminal","title":"terminal","text":""},{"location":"api/#metatrader5_wrapper.terminal.TerminalInfo","title":"TerminalInfo","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed terminal details returned by <code>MetaTrader5.terminal_info()</code>.</p>"},{"location":"api/#metatrader5_wrapper.terminal.TerminalVersion","title":"TerminalVersion","text":"<p>               Bases: <code>MT5NamedTupleModel</code></p> <p>Typed terminal version payload returned by <code>MetaTrader5.version()</code>.</p>"},{"location":"api/#metatrader5_wrapper.terminal.shutdown","title":"shutdown","text":"<pre><code>shutdown() -&gt; OperationResult[None]\n</code></pre> <p>Cleanly disconnect from the MetaTrader 5 terminal.</p> <p>Closes the active MT5 session and releases resources. Should be called when your application is finished trading or connecting to the terminal.</p> <p>Returns:</p> Type Description <code>OperationResult[None]</code> <p>OperationResult[None] indicating the shutdown completed successfully.</p> <p>Example::</p> <pre><code>result = shutdown()\nif result.success:\n    print(\"Terminal closed\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\terminal\\service.py</code> <pre><code>def shutdown() -&gt; OperationResult[None]:\n    \"\"\"Cleanly disconnect from the MetaTrader 5 terminal.\n\n    Closes the active MT5 session and releases resources. Should be called when\n    your application is finished trading or connecting to the terminal.\n\n    Returns:\n        OperationResult[None] indicating the shutdown completed successfully.\n\n    Example::\n\n        result = shutdown()\n        if result.success:\n            print(\"Terminal closed\")\n    \"\"\"\n    logger.debug(\"Shutting down MetaTrader 5 terminal.\")\n    mt5_connection.shutdown_terminal()\n    logger.info(\"MetaTrader 5 terminal shutdown completed.\")\n    return OperationResult[None](\n        success=True,\n        message=\"MetaTrader 5 terminal shutdown completed.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.terminal.terminal_info","title":"terminal_info","text":"<pre><code>terminal_info() -&gt; OperationResult[TerminalInfo]\n</code></pre> <p>Fetch MetaTrader 5 terminal information and status.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalInfo]</code> <p>OperationResult[TerminalInfo] with terminal metadata including connection</p> <code>OperationResult[TerminalInfo]</code> <p>status, trading permissions, paths, language, and company name.</p> <p>Example::</p> <pre><code>result = terminal_info()\nif result.success and result.data:\n    print(f\"Terminal: {result.data.name}\")\n    print(f\"Build: {result.data.build}\")\n    print(f\"Connected: {result.data.connected}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\terminal\\service.py</code> <pre><code>def terminal_info() -&gt; OperationResult[TerminalInfo]:\n    \"\"\"Fetch MetaTrader 5 terminal information and status.\n\n    Returns:\n        OperationResult[TerminalInfo] with terminal metadata including connection\n        status, trading permissions, paths, language, and company name.\n\n    Example::\n\n        result = terminal_info()\n        if result.success and result.data:\n            print(f\"Terminal: {result.data.name}\")\n            print(f\"Build: {result.data.build}\")\n            print(f\"Connected: {result.data.connected}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 terminal info.\")\n    result = mt5_terminal.terminal_info()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 terminal info: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[TerminalInfo](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 terminal info.\",\n        )\n\n    terminal = model_from_namedtuple(TerminalInfo, result.data)\n    logger.info(\"MetaTrader 5 terminal info fetched successfully.\")\n    return OperationResult[TerminalInfo](\n        success=True,\n        data=terminal,\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 terminal info fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#metatrader5_wrapper.terminal.version","title":"version","text":"<pre><code>version() -&gt; OperationResult[TerminalVersion]\n</code></pre> <p>Fetch MetaTrader 5 terminal version and build information.</p> <p>Returns:</p> Type Description <code>OperationResult[TerminalVersion]</code> <p>OperationResult[TerminalVersion] with version number, build number, and</p> <code>OperationResult[TerminalVersion]</code> <p>release date.</p> <p>Example::</p> <pre><code>result = version()\nif result.success and result.data:\n    print(f\"Version: {result.data.version}.{result.data.build}\")\n    print(f\"Released: {result.data.release_date}\")\n</code></pre> Source code in <code>src\\metatrader5_wrapper\\terminal\\service.py</code> <pre><code>def version() -&gt; OperationResult[TerminalVersion]:\n    \"\"\"Fetch MetaTrader 5 terminal version and build information.\n\n    Returns:\n        OperationResult[TerminalVersion] with version number, build number, and\n        release date.\n\n    Example::\n\n        result = version()\n        if result.success and result.data:\n            print(f\"Version: {result.data.version}.{result.data.build}\")\n            print(f\"Released: {result.data.release_date}\")\n    \"\"\"\n    logger.debug(\"Fetching MetaTrader 5 terminal version.\")\n    result = mt5_terminal.version()\n    if not result.success:\n        logger.warning(\n            \"Unable to fetch MetaTrader 5 terminal version: [%s] %s\",\n            result.error.code,\n            result.error.message,\n        )\n        return OperationResult[TerminalVersion](\n            success=False,\n            error_code=result.error.code,\n            error_message=result.error.message,\n            message=\"Unable to fetch MetaTrader 5 terminal version.\",\n        )\n\n    version_number, build, release_date = cast(tuple[int, int, str], result.data)\n    logger.info(\"MetaTrader 5 terminal version fetched successfully.\")\n    return OperationResult[TerminalVersion](\n        success=True,\n        data=TerminalVersion(\n            version=version_number,\n            build=build,\n            release_date=release_date,\n        ),\n        error_code=result.error.code,\n        error_message=result.error.message,\n        message=\"MetaTrader 5 terminal version fetched successfully.\",\n    )\n</code></pre>"},{"location":"api/#next-steps","title":"Next Steps","text":"<p>Getting Started</p>"},{"location":"guides/client-vs-functions/","title":"Client vs Functions","text":""},{"location":"guides/client-vs-functions/#next-steps","title":"Next Steps","text":"<p>Connection</p> <p>The package supports two public styles.</p>"},{"location":"guides/client-vs-functions/#start-with-the-client","title":"Start with the client","text":"<p>Use <code>MetaTrader5Client</code> first unless you have a reason not to. It gives you a clean lifecycle boundary and the shortest happy-path code.</p>"},{"location":"guides/client-vs-functions/#new-features","title":"New Features","text":"<p>The package now includes support for historical orders and deals.</p> <pre><code>from metatrader5_wrapper import MetaTrader5Client\n\nwith MetaTrader5Client(credentials) as mt5:\n    account = mt5.account()\n    positions = mt5.positions(symbol=\"EURUSD\")\n    history_orders = mt5.history_orders_get(datetime(2021, 1, 1), datetime(2021, 1, 10))\n    history_deals = mt5.history_deals_get(datetime(2021, 1, 1), datetime(2021, 1, 10))\n</code></pre> <p>This style is usually best for application code, services, bots, notebooks, and scripts that should read cleanly from top to bottom.</p>"},{"location":"guides/client-vs-functions/#function-style-api","title":"Function-style API","text":"<p>Use module-level functions if you want a minimal wrapper over the MT5 API shape.</p> <pre><code>from metatrader5_wrapper import account_info, initialize, shutdown\n\ninitialize()\naccount = account_info()\nshutdown()\n</code></pre> <p>This style is useful when migrating existing procedural MT5 code.</p>"},{"location":"guides/client-vs-functions/#which-one-should-you-document-first","title":"Which one should you document first","text":"<p>Lead with <code>MetaTrader5Client</code> in tutorials and examples because it gives users the cleanest mental model. Keep function-style examples in the API reference and migration-oriented sections.</p>"},{"location":"guides/connection/","title":"Connection","text":""},{"location":"guides/connection/#next-steps","title":"Next Steps","text":"<p>Results Model</p> <p>The wrapper keeps connection logic explicit. MetaTrader 5 initialization and account login are treated as separate stages so failures can be reported with a clear <code>stage</code>, <code>error_code</code>, and <code>error_message</code>.</p>"},{"location":"guides/connection/#initializecredentialsnone","title":"<code>initialize(credentials=None)</code>","text":"<p><code>initialize()</code> starts the MetaTrader 5 terminal. When credentials are provided, the wrapper initializes the terminal first and then performs login.</p> <pre><code>from metatrader5_wrapper import initialize\n\nresult = initialize()\n</code></pre> <p>With credentials:</p> <pre><code>from metatrader5_wrapper import LoginCredential, initialize\nfrom pydantic import SecretStr\n\ncredentials = LoginCredential(\n    login=12345678,\n    password=SecretStr(\"password\"),\n    server=\"Broker-Demo\",\n)\n\nresult = initialize(credentials)\n</code></pre>"},{"location":"guides/connection/#logincredentials","title":"<code>login(credentials)</code>","text":"<p>Use <code>login()</code> when the terminal is already initialized and you want an explicit login step.</p>"},{"location":"guides/connection/#shutdown","title":"<code>shutdown()</code>","text":"<p>Always shut down the MT5 session when you are done, unless your application is intentionally keeping the terminal open.</p> <pre><code>from metatrader5_wrapper import shutdown\n\nshutdown_result = shutdown()\n</code></pre>"},{"location":"guides/connection/#failure-handling","title":"Failure handling","text":"<p>Connection failures return <code>ConnectionResult</code> instead of raising automatically. That makes it straightforward to surface the raw MT5 error while keeping a consistent application flow.</p> <pre><code>result = initialize(credentials)\n\nif result.failed:\n    print(result.stage)\n    print(result.error_code)\n    print(result.error_message)\n</code></pre>"},{"location":"guides/positions/","title":"Positions","text":""},{"location":"guides/positions/#next-steps","title":"Next Steps","text":"<p>Results Model</p> <p>The positions API exposes the current open positions in the connected MT5 session.</p>"},{"location":"guides/positions/#total-open-positions","title":"Total open positions","text":"<pre><code>from metatrader5_wrapper import positions_total\n\nresult = positions_total()\n</code></pre>"},{"location":"guides/positions/#read-positions","title":"Read positions","text":"<pre><code>from metatrader5_wrapper import positions_get\n\nresult = positions_get(symbol=\"EURUSD\")\n\nif result.success and result.data is not None:\n    for position in result.data:\n        print(position.ticket, position.symbol, position.volume)\n</code></pre>"},{"location":"guides/positions/#filtering-behavior","title":"Filtering behavior","text":"<p>The current implementation uses one filter at a time in this order:</p> <ul> <li><code>symbol</code></li> <li><code>group</code></li> <li><code>ticket</code></li> <li>no filter</li> </ul> <p>If you need exact behavior details, see the generated API reference.</p>"},{"location":"guides/results/","title":"Results Model","text":"<p>The package avoids exposing raw MT5 return values directly. Instead, public operations return typed result objects with explicit success and error state.</p>"},{"location":"guides/results/#new-features","title":"New Features","text":"<p>The package now includes support for historical orders and deals.</p>"},{"location":"guides/results/#operationresultt","title":"<code>OperationResult[T]</code>","text":"<p>Most read operations return <code>OperationResult[T]</code>.</p> <p>Typical fields:</p> <ul> <li><code>success</code></li> <li><code>data</code></li> <li><code>error_code</code></li> <li><code>error_message</code></li> <li><code>message</code></li> <li><code>failed</code></li> <li><code>describe_error()</code></li> <li><code>unwrap()</code></li> <li><code>expect(message)</code></li> </ul> <p>Example:</p> <pre><code>from metatrader5_wrapper import account_info\n\nresult = account_info()\n\nif result.success and result.data is not None:\n    print(result.data.balance)\nelse:\n    print(result.error_code, result.error_message)\n</code></pre> <p>Fail-fast helpers:</p> <pre><code>from metatrader5_wrapper import account_info\n\naccount = account_info().expect(\"Account is required before placing orders\")\nprint(account.balance)\n</code></pre> <p>Use these helpers when failure should immediately stop the current flow. Keep checking <code>success</code> manually when you want to recover or branch.</p>"},{"location":"guides/results/#connectionresult","title":"<code>ConnectionResult</code>","text":"<p>Connection operations return <code>ConnectionResult</code>, which includes an additional <code>stage</code> field to show whether the failure happened during initialize or login.</p> <pre><code>from metatrader5_wrapper import initialize\n\nresult = initialize()\n\nif result.failed:\n    print(result.describe_error())\n</code></pre>"},{"location":"guides/results/#why-this-matters","title":"Why this matters","text":"<p>The official MT5 API exposes failures through <code>last_error()</code>, which can be overwritten by the next call. This wrapper captures that error state immediately and binds it to the returned result object.</p>"},{"location":"guides/results/#historical-orders-and-deals","title":"Historical Orders and Deals","text":"<p>The new historical functions return results wrapped in <code>OperationResult</code> with typed models for <code>HistoryOrder</code> and <code>HistoryDeal</code>.</p>"},{"location":"guides/results/#next-steps","title":"Next Steps","text":"<p>Symbols and Ticks</p>"},{"location":"guides/symbols/","title":"Symbols and Ticks","text":"<p>The symbols API exposes typed access to instrument metadata and live tick data.</p>"},{"location":"guides/symbols/#browse-symbols","title":"Browse symbols","text":"<pre><code>from metatrader5_wrapper import symbols_get\n\nresult = symbols_get(group=\"*USD*\")\n\nif result.success and result.data is not None:\n    print(len(result.data))\n</code></pre>"},{"location":"guides/symbols/#inspect-one-symbol","title":"Inspect one symbol","text":"<pre><code>from metatrader5_wrapper import symbol_info\n\nresult = symbol_info(\"EURUSD\")\n\nif result.success and result.data is not None:\n    print(result.data.name, result.data.digits, result.data.spread)\n</code></pre>"},{"location":"guides/symbols/#read-the-latest-tick","title":"Read the latest tick","text":"<pre><code>from metatrader5_wrapper import symbol_info_tick\n\ntick = symbol_info_tick(\"EURUSD\")\n\nif tick.success and tick.data is not None:\n    print(tick.data.bid, tick.data.ask)\n</code></pre>"},{"location":"guides/symbols/#add-a-symbol-to-marketwatch","title":"Add a symbol to MarketWatch","text":""},{"location":"guides/symbols/#from-metatrader5_wrapper-import-symbol_select-result-symbol_selecteurusd-enabletrue","title":"<pre><code>from metatrader5_wrapper import symbol_select\n\nresult = symbol_select(\"EURUSD\", enable=True)\n</code></pre>","text":""},{"location":"guides/symbols/#next-steps","title":"Next Steps","text":"<p>Positions</p>"}]}